# Developer documentation

This file describes how the Smart Parks Protect codebase works today. The plan for where it is going lives in `PROJECT_PLAN.md`. The product and architecture rationale lives in `Smart_Parks_Protect_Concept_Architecture.md`. Conventions live in `CONVENTIONS.md`.

Status: v2.6.0 released on 2026-09-16; phases 0 to 28 are done (phases 22 and 28, the analysis modules and device performance, went out in the same tag). Phase 29 is open and unreleased: its landscape half (the Sentinel-2 vegetation of the grazing analysis, P4 to P6) is built and running on the dev server, its movement half (P1 to P3) is not started. `main` is ahead of the tag by the work of 2026-09-17 and 2026-09-18 and is what the dev server runs. A few live-verification items of phases 7 to 9 and the phase 23 exit criteria still wait for accounts or for Tim's reading; `PROJECT_PLAN.md` carries the phases, the decisions and the session log.

## Project overview

**Smart Parks Protect** is a self-hosted operational data platform that:
- Ingests data from field devices and IoT platforms (OpenCollar over LoRaWAN first, then Traccar, Iridium via Cloudloop and Rock7, generic MQTT and HTTP, and detections from AddaxAI Connect)
- Normalizes everything into one Smart Parks domain: Entities, Devices, Positions, Measurements, Events
- Provides a live map, a Data Explorer, exports, a stateful rules engine, device control and outbound integrations such as EarthRanger

**Architecture:** services in a monorepo, orchestrated with Docker Compose, Redis Streams as the event bus, PostgreSQL 17 with PostGIS and TimescaleDB, MinIO for files.
**Deployment:** local docker compose during early phases, then Ubuntu VM with Ansible.
**Design envelope:** 25,000 registered devices, 10,000 reporting, 5,000 entities on the map, 250 source events per second sustained, 250 million positions and 1 billion measurements online, 100 concurrent users. These are architecture targets, not current capacity.
**Relationship to AddaxAI Connect:** written from scratch; AddaxAI Connect is the reference for patterns (auth flow, RBAC, deployment, notifications, frontend conventions). A local clone is expected at `/home/tim/apps/AddaxAI-Connect` for comparison.

## Working with the plan

- Read `PROJECT_PLAN.md` at the start of every session and work the next unchecked item.
- Tick the checkbox in the same commit as the code and the docs.
- Add a session log entry at the end of every session.
- Update this file whenever a mechanism, convention or command changes.

## Stack

| Layer | Technology | Notes |
| --- | --- | --- |
| Database | PostgreSQL 17 + PostGIS + TimescaleDB (`timescale/timescaledb-ha:pg17.10-ts2.29.2`) | Hypertables for positions, measurements, source events, gateway receptions. Decision gate in phase 4 |
| Event bus and cache | Redis 7.4, Redis Streams with consumer groups | One broker; `RedisStreamsBus` in `shared/bus.py`, `Worker` base in `shared/worker.py` |
| Object storage | MinIO | Raw log files, uploads, exports. Not for telemetry |
| Backend | Python 3.12, FastAPI, SQLAlchemy 2 async, Alembic, Pydantic 2, FastAPI-Users | `uv` workspace, one `uv.lock`, exact `==` pins in every `pyproject.toml` |
| Frontend | React 19, Vite, TypeScript strict, Tailwind 4, shadcn/ui (Radix), TanStack Query, Zustand, React Hook Form + Zod, React Router 8 | MapLibre GL JS 6 for maps, Apache ECharts 6 for charts, terra-draw for drawing. Rules in `services/frontend/FRONTEND_CONVENTIONS.md` |
| Tests | pytest, Vitest, Playwright (phase 3) | API tests run against real Postgres, Redis and MinIO |
| Docs | MkDocs 1.x with Material, ADRs in `docs/adr/` | `mkdocs build --strict` in CI. MkDocs 2 removes plugins and is pinned out |
| Deployment | Docker Compose, Ansible, Nginx, Let's Encrypt | From phase 7 |

## Repository structure

What exists today. The full target tree is in `PROJECT_PLAN.md` under "Target repository structure".

```
smartparks-protect/
├── services/
│   ├── api/                 # FastAPI service `protect_api`: auth/, routers/, schemas/, deps.py, alembic/, bootstrap.py
│   ├── ingest/              # `protect_ingest`: runs adapter event connectors (MQTT, polling, websocket)
│   ├── decoder/             # `protect_decoder`: source events to canonical rows
│   ├── export/              # `protect_export`: export jobs to MinIO
│   ├── rules/               # `protect_rules`: rule evaluation, scheduler, system checks
│   ├── automation/          # `protect_automation`: actions, notification delivery, Telegram poller
│   ├── integration/         # `protect_integration`: outbound deliveries, retry loop, backfill
│   ├── mcp/                 # `protect_mcp`: MCP server for AI clients, calls the API with the client's token
│   └── frontend/            # React + Vite + TypeScript, nginx Dockerfile, FRONTEND_CONVENTIONS.md
├── shared/shared/           # package `shared`: config, database, logger, version, enums, permissions,
│                            # models/, domain/assignments.py, domain/links.py, catalog/, curation/, integrations/,
│                            # metrics/, records.py, pictures.py, logfiles.py, backup.py, ratelimit.py,
│                            # telemetry.py, connectivity/gateway_sync.py, trace.py, timeutil.py, secrets.py, bus.py,
│                            # worker.py, ingest.py, storage.py, connectivity/ (base, transports, adapters),
│                            # device_drivers/ (base, registry, generic_json, opencollar), analytics.py, exports/,
│                            # rules/ (schema, templates, evaluator, data, events, replay), notifications/,
│                            # control/ (actions, commands), oauth.py (scopes, access tokens)
├── ansible/                 # playbook, roles, *.example inventory and vars (phase 7)
├── tests/                   # tests/shared, tests/api, tests/fixtures/payloads
├── docs/                    # MkDocs site, docs/adr/ holds the ADRs
├── docker/python.Dockerfile # one image for every Python service; compose sets the command per service
├── docker/chirpstack/       # ChirpStack, gateway bridge and Mosquitto config for the compose profile
├── docker/postgres/         # pgBackRest config and the protect-pgbackrest wrapper
├── docker/backup/           # object mirror script, compose overrides of the restore-test stack
├── examples/                # adapter, driver and payload skeletons to copy from
├── scripts/dev.sh           # daily commands; backup.sh, restore.sh, restore-verify.sh, verify-server.sh for servers
├── .githooks/commit-msg     # strips assistant trailers (D28), installed with scripts/dev.sh hooks
├── .github/workflows/ci.yml
├── docker-compose.yml
├── pyproject.toml           # uv workspace root, ruff, mypy, pytest config
└── uv.lock
```

The rules behind it:

- `services/<name>/` is one Python package each (`protect_<name>`), all built into one image (`docker/python.Dockerfile`) and started with a different command per compose service. Services import from `shared` and never from each other.
- `shared/shared/` is the only place for models, schemas, the bus, the trace helpers, device drivers and connectivity adapters.
- Provider-specific code lives only in `shared/connectivity/adapters/<provider>/`. Device-specific code lives only in `shared/device_drivers/<family>/`. A test enforces this from phase 7.
- `tests/<package>/` mirrors the packages; CI runs one job per directory. `tests/fixtures/payloads/` holds recorded real payloads with a note where each came from.

## Core concepts for developers

- **Device versus Entity.** A Device is hardware and exists at server level without a project column. An Entity is the real-world object (animal, vehicle, gate). They are linked by `device_entity_assignments` with a validity range. Project membership of a device is `device_project_assignments`, also time-bounded. Both use `tstzrange` with an exclusion constraint so ranges never overlap.
- **Attribution uses canonical time.** A record belongs to the project and entity that were assigned at the record's device-origin time, not at ingest time. `shared/domain/assignments.py` is the one place that resolves this (`resolve_attribution(session, device_id, at)`); `rewrite_attribution(session, device_id, start, end)` rewrites the project and entity of a device's positions and measurements over a window from the assignments as they stand (decision D103; it decompresses TimescaleDB chunks in the transaction), and `reattribute` adds the current-state recompute for the one small window the curation overlay rewrites. Every assignment change (a start moved back, a device assigned to a project or an entity, a handover, a changed assignment, "Recompute attribution", the bulk assignment) queues an attribution job instead (decision D206, ADR 0032): `shared/domain/attribution.py` holds the `AttributionJob` rows (`attribution_jobs`, migration 0034), `queue_reattribution` (a change while the device's job is still queued folds into it and widens its window; a running job raises `AttributionBusy`, a 409 at the API through `protect_api/attribution.py`; a window without records queues nothing), `publish_job` after the commit on `attribution.requested`, and `run_attribution_job`, the export service's handler, which rewrites the window in pieces of `WINDOW_DAYS` (30) with `records_done` on the row after each and recomputes the current state once at the end. `GET /devices/{id}/attribution-jobs` lists a device's newest jobs and the assignment endpoints answer the job as `attribution_job`; `hooks/useAttributionJob.ts` polls it every 3 s while one is active, toasts the outcome and invalidates the page's queries, `components/devices/AttributionProgress.tsx` is the callout with the bar on the device and entity pages, and the assignment buttons of that device are disabled meanwhile. `GET /devices/{id}/data-span` tells the dialogs and the device page when the device first produced data and how many records sit before its assignments; `POST .../project-assignments/{id}/extend-start` and `POST /projects/{id}/entity-assignments/{id}/extend-start` are the repair for records before an assignment, and `POST /devices/{id}/reattribute` (the device page's "Recompute attribution") the repair for records inside one that were decoded before it existed; creating a project or entity assignment and a handover call `reattribute` over the new range as well, so records decoded before the assignment existed get it at once. `POST /projects/{id}/entity-assignments` takes `entity_id` or `new_entity` (an `EntityCreate` made in the same transaction through `_new_entity`, decision D170), so the device page assigns to an entity that does not exist yet; `components/devices/AssignEntityDialog.tsx` is that dialog and `components/entities/AssignDeviceDialog.tsx` the entity's side, which the entities list opens per untracked row.
- **Entity types with sub-types.** `entity_types.parent_id` (migration 0025, ADR 0025, decisions D166 to D169) makes a row a sub-type of a type, one level deep (`_check_parent` in `routers/catalog.py`; a type with sub-types cannot become one, deleting a type with sub-types is a 409). `shared/catalog/entity_types.py` is the standard catalogue (six types, a sub-type per vendored icon, keys from the icon names) with `seed_sql()` for the migration, `ON CONFLICT DO NOTHING` so hand-made rows stay. A project hides types in `settings.hidden_entity_type_ids`; the frontend applies it in `lib/entityTypes.ts` (`visibleTypes`, a hidden type takes its sub-types) and `components/entities/EntityTypeSelect.tsx` is the type plus sub-type control the entity dialog and the bulk dialogs use; `ProjectSettingsPage` has the switches. `DeliveryItem.entity_type_parent_key` carries the type of a sub-type and both EarthRanger connectors fall back to it as the subject type.
- **DataSource and ExternalIdentity.** A DataSource is an external platform account (a ChirpStack instance, a KPN account). ExternalIdentity maps `(data_source, external_id)` such as a DevEUI to a Device. Incoming data is resolved through this pair. Unknown identities are retained and shown in Needs Attention, never guessed.
- **Four data levels.** Raw SourceEvent (immutable), decoded (driver output), normalized canonical rows (positions, measurements, states, events), aggregated (server-side buckets). Maps, charts, rules and exports use canonical rows.
- **Canonical key and deduplication.** For OpenCollar: device EUI + device-origin timestamp + record type, plus a payload fingerprint where needed. Several deliveries (LoRaWAN, WebBLE, log file, Iridium) link to one canonical row. Never use network, satellite, sync, upload or ingest time in the key.
- **ProcessingTrace.** Every flow (inbound message, command, import, delivery, export) has a trace id and ordered steps with status and a structured ApplicationError on failure. Successful routine telemetry writes compact traces. `shared/trace.py` has the `Tracer`; a step is `async with tracer.step(component, operation)`; raise `ApplicationError(code, message, component, ...)` for expected failures, anything else is recorded as `INTERNAL_ERROR` and re-raised.
- **Bounded queries.** Every user-facing list, map, chart and export endpoint has an explicit bound. An endpoint that could return the whole history is a defect.

## Conventions specific to this repo

### Timestamps

- Every time column is `TIMESTAMPTZ` and stored in UTC. No naive datetimes anywhere. A helper raises on a naive datetime.
- Canonical rows have `time` (device-origin, defined by the driver per record type) and `ingested_at`.
- SourceEvents keep provenance times separately: `network_received_at`, `satellite_delivered_at`, `ble_synced_at`, `file_uploaded_at`, `ingested_at`.
- Display timezone is a user or project setting applied in the frontend and in exports on request. UTC is the export default.

### Event bus

`shared/bus.py`, Redis Streams (ADR 0004). Topics are `<object>.<verb>` in past tense, listed on `Topic`: `source_event.received`, `position.created`, `measurement.created`, `device.state_changed`, `event.created`, `alert.created`, `command.updated`, `delivery.updated`, `needs_attention.created`.

- Publish after the database commit, never before (`shared.ingest.commit_and_publish`, `protect_decoder.pipeline.publish_outcome`), so a consumer never reads a row that is not committed.
- Each worker is one consumer group (`Worker(name)` in `shared/worker.py`; `worker.subscribe(topic, handler)`). A handler acknowledges by returning. A handler that raises leaves the message pending; it is re-delivered after `BUS_RETRY_BASE_SECONDS` doubling per attempt, and dead-lettered to `<topic>.dead` after `BUS_MAX_ATTEMPTS`. An `ApplicationError` with `retryable=False` is dead-lettered at once. Pending messages of a crashed consumer are reclaimed by any consumer of the group.
- Messages carry `schema_version`; a newer version than the consumer knows is dead-lettered with `SCHEMA_VERSION_UNSUPPORTED`.
- Streams are trimmed to about `BUS_MAXLEN` entries (D33). Dead-letter streams are listed, retried and resolved through `/api/v1/attention/dead-letters`. Unknown identities become devices one at a time (`/attention/identities/{id}/create-device`) or many at once (`/attention/identities/bulk-create-devices`, D96: one device type, an optional project and entity type, names from the identity's `name` attribute, which the KPN adapter fills from ThingPark's device name, else the external id; `bulk-ignore` for the rest); linking an identity (create, bulk create, link, reprocess) does not republish the events: `shared.ingest.queue_identity_reprocess` marks every retained event of the identity received with the device in one statement, gives the identity's gateway receptions the device (`link_receptions`; migration 0022 repaired the rows from before), commits, and publishes one `identity.reprocess_requested` message; the decoder's `on_identity_reprocess` walks those events oldest first in pages (keyset on `ingested_at, id`), each decoded and committed on its own, so a redelivery carries on with what is left and a failing event, marked failed by the pipeline, does not stop the rest (decision D121). Progress is the summary's `queued_source_events` (received events with a device); the Needs attention page polls it every 3 s while it is above zero. `POST /devices/bulk-assign` (D122, server admins) gives a selection of devices one project from each device's first data (`_first_data_at`: records, identity first seen, log files) or a given start, an optional entity each, and reattributes the records in the range (D103); the all-scope devices list carries the selection and the `BulkAssignDialog`. The shared `DataTable` has an optional `selection` column for such pages, and `columnFilters` adds a filter row under the header (a choice of the column's faceted values up to a dozen, else a text match; `meta.filter` on a column forces `"select"`, `"text"` or `false` for dates and counts), with the header box selecting the rows that pass every filter.
- Every worker stamps `heartbeat:<worker>` each loop; `HEARTBEAT_STALE_MINUTES` (15) without a stamp is stale. `/api/v1/attention/summary` reads it, and the rules service turns a stale worker into a system alert.
- Tests use Redis database 1, flushed per session; the development stack uses database 0.

### Ingestion

`shared/ingest.py` is the one inbound path: resolve the external identity (create it unknown if new), store the source event (payload inline up to `PAYLOAD_INLINE_MAX_BYTES`, else in the MinIO uploads bucket with key, size and SHA-256 on the row, D32), start a compact trace, and after the commit publish `source_event.received`, or `needs_attention.created` for an unknown or ignored identity. Two callers: `POST /api/v1/ingest/http/{data_source_id}` (bearer token per source, D34; token returned once at creation or by `POST /data-sources/{id}/webhook-token`; a platform that signs its pushes itself is verified by the adapter's optional `verify_webhook(source, body, headers, query)` with the source's credentials when no valid bearer is present, ThingPark's Token with the AS key, D95; `query` comes from `raw_query_params`, which keeps the `+` of a time offset that the framework's parser would turn into a space; a source's `allowed_source_ips` is checked against `client_address`, the proxy's view of the caller: `X-Real-IP`, else the last address of `X-Forwarded-For`, never the first, which the caller can forge) and the ingest service, which runs the event connectors of enabled data sources and re-reads the sources every minute.

### Satellite sessions, connectivity and network locations

`shared/connectivity/satellite.py` (ADR 0023, decisions D158 to D160): `SatelliteSession` is the one shape of an Iridium session (status mapped from the DirectIP codes and Cloudloop's names, `sequence` MOMSN, `mt_sequence` MTMSN, the estimate with `cep_km`, `bytes`, `session_at`); the Cloudloop and Rock7 adapters put it on `InboundMessage.satellite_session`. `store_inbound` stores it as `provider_metadata["satellite_session"]` and runs `_satellite_session`: a delivery with the same sequence and payload as one of the identity in the last seven days is the platform's retry, stored as `duplicate` and not published; a counter jump becomes `missed_since_last`; the identity's `attributes["satellite"]` remembers the last sequence; every satellite delivery gets the trace step "satellite session". The decoder notes a fix more than three CEP radii from the estimate (`_note_estimate_disagreement`), writes the session to `ConnectivityState.attributes["satellite"]`, and calls `apply_satellite_delivery` (an MTMSN above zero moves the oldest pending command of the route to transmitted). `TrafficRow.satellite` carries the session.

Network health is apart from device health (architecture 20, decision D161, ADR 0024): `GET /devices/{id}/connectivity` (`device_connectivity` in `routers/devices.py`) answers one `SourceConnectivity` block per data source the device has an identity on, from the `ConnectivityState` row (state, last uplink, join and downlink, last RSSI and SNR), the receptions of the period (`_lorawan_link`: gateways, the best one and its share, mean signal, the frame counter gaps read from `provider_metadata.f_cnt`) and the satellite sessions of the period (`_iridium_link`: the last session from the connectivity state's `attributes.satellite`, sessions, bytes, missed, redeliveries); `components/devices/ConnectivityCard.tsx` renders it on the Connectivity tab of the device and entity pages. The health card (`device_health`) is the device's own status only.

Network locations (decisions D162 to D164): `shared/connectivity/network_location.py` defines `NetworkLocation`; the Cloudloop and Rock7 sessions the network stands by become one through `from_satellite` in `store_inbound`, the KPN adapter builds one from `DevEUI_location` (`DevLAT`, `DevLON`, `DevAlt`, `DevLocRadius`, `DevLocTime`, the algorithm) and the TTS adapter from `location_solved`; the ingest stores it as `provider_metadata["network_location"]`, and the decoder's `_add_network_position` writes it as a position with `record_type` `network` (`NETWORK_RECORD_TYPE`), the radius as `accuracy_m`, the method in the attributes; `position.created` names the `record_type`. `_apply_position` in the pipeline decides the current position of the device and the entity from `location_source` and `location_fallback_hours` (migration 0024, `LocationSource`): a device fix newer than `latest_fix_time` always takes it; a network location only under `network` or, under `device_else_network`, when no fix arrived within the fallback period before it; `latest_position_kind` says which it is and the map's features carry it as `position_kind`. Readers: `device_fix()` and `sources_filter()` in `shared/curation/effective.py`; the positions list, the tracks and the records read take `sources` (`device` by default, `network`, `all`), the heatmap and the coverage SQL filter `record_type <> 'network'`, `position_at` prefers the fix, the rules' replay takes device fixes and the live rules skip a network position unless the entity opted in, `matches` in `shared/integrations/deliveries.py` refuses a network position unless the integration's `config.include_network_positions` is true, exports keep every row. `GET /projects/{id}/map/network-locations` lists the network positions in the window and view with their radius, which `components/map/networkLocations.ts` turns into circles for the "Network locations" row of the Coverage tab (`layerChoices.network_locations`); `components/devices/LocationSourceCard.tsx` is the setting on the Overview cards; the explore strip's "Network locations" switch sets `sources=all` on the records read. The accuracy of the position shown travels with it (D193): `_set_position` copies the record's `accuracy_m` into `latest_accuracy_m` on both current states (migration 0028), the map's entity and device features and the entity tile carry `accuracy_m`, and the frontend draws a disc of that radius under the marker above `ACCURACY_WARN_M` (50 m, `lib/accuracy.ts`; `accuracyCircles` in `components/map/layers.ts`) and warns in the panels.

### Decoding

`services/decoder/protect_decoder/pipeline.py`: driver from the device type, decode, canonical key per record, repeat deliveries link to the existing row in `source_deliveries`, attribution at the record's time, canonical rows and current state in one transaction, domain events after commit. Unknown metric keys are registered automatically in the metric registry with category `uncategorized`; `GET /attention/metrics` lists them with the devices reporting each (from the current state's `latest_measurements`) and the categories in use, the Needs attention page defines them through `PATCH /metrics/{key}`, and the summary counts them (D102). The decoder keeps on `device_current_state` the newest value per metric (`latest_measurements`, key to value and time), the merged `latest_state` with `latest_state_time`, `last_seen_at` from any record, and writes the firmware version a status reports on the device row (migration 0016, D104); `shared/domain/battery.py` (decision D248) is what a battery voltage means: a `BatteryProfile` per chemistry (primary lithium, lithium-ion, LiFePO4, two alkaline cells) with its warn and critical thresholds and a voltage-to-charge curve, and `resolve(attributes, default_settings, driver)`, which takes the type a person set on the device (`devices.attributes["battery_type"]`), else the device type's `default_settings["battery_type"]`, else the driver's `default_battery_type` (OpenCollar: primary lithium), else none. `device_health(..., battery=profile)` judges the `battery_voltage` line by that chemistry instead of the driver's one-size `HealthField` bounds and puts the share of charge on the line; `GET` and `PUT /devices/{id}/battery` read and set it (project admins of the device's current project, or a server admin) behind `components/devices/BatteryCard.tsx`, and the map features carry `battery_percent` and `battery_type` so a panel reads "3.72 V · 64%" with the chemistry behind the value. Without a type nothing changes. `shared/domain/health.py` derives `DeviceHealth` from that and the driver's `health` fields for the device reads (`with_state` in `routers/devices.py`, which also names the data sources of the device's identities in `data_source_names`) and the map's current-state features. `GET /devices` filters on `project_id`, `in_no_project`, `status_filter`, `data_source_id` (an identity on that source), `device_type_id`, `has_entity`, `unassigned` and `group_id`, and `q` matches the name, the serial and any identity; the server admin's devices page (`pages/admin/AdminDevicesPage.tsx`) keeps its filters in the URL and adds the DataTable's filter row over the loaded rows. Health is derived per row, so it is a column filter, not a server filter. A LoRaWAN uplink without application payload (port 0, a MAC-only frame) and a port the driver does not know are not failures: the pipeline and the driver put a note on `DecodedRecords.notes`, the trace step shows it, the source event stays `processed` and the connectivity and current state are updated as for any uplink. A record whose device time runs more than `CLOCK_AHEAD_TOLERANCE_SECONDS` ahead of its delivery (`shared.timeutil.clock_ahead`, decision D119) is written with `valid=False`, counted on the outcome and the "canonical rows written" step, and left out of the current state, so a broken clock never blocks real updates; `GET /attention/clock-ahead` lists such devices from the chunks after today (`time > now()`), and the curation job's time offset repairs them. GNSS outliers (decisions D221 to D224, `shared/domain/outliers.py`): `_write_positions` judges every own fix against the device's last valid fix before it (`_previous_fix`, through the effective time and geometry, so a fix written earlier in the same transaction counts) with `outlier_of` (farther than `OUTLIER_MIN_JUMP_M` and faster than `OUTLIER_MAX_SPEED_MPS`, settings with 1 km and 50 m/s as defaults); a flagged fix is written `valid=False` with the figures under `attributes.outlier`, counted on the outcome (`outliers`, `outlier_times`) and the trace step, left out of the current state, and gets a `position_outlier` event of severity info; an invalid position (a clock ahead, an outlier) publishes no `position.created`, so the map, the rules and the integrations never see it. `shared/curation/jobs.py` has the job kind `flag_outliers` (`find_outliers` walks a period's valid fixes per device in time order with the same rule; the preview counts what it would flag, the apply writes set-valid corrections with the figures on the rows), `GET /projects/{id}/curation/outliers` lists the flagged fixes (`OutlierRead`, `waiting`), a set-valid correction with the reason `OUTLIER_APPROVED` (migration 0037) approves one, and `GET /devices/{id}/data-span` carries `outliers_waiting`; the Curation page's Outliers tab and the device page's notice are the interface. See `docs/architecture/processing-pipeline.md` and `docs/analytics/curation.md`.


Movement (decision D204): `shared/domain/movement.py` turns the accelerometer sample of a status message (`acceleration_x/y/z`) into the `activity` measurement, the change of the vector against the sample before it, which the decoder derives before the measurements are written (the previous sample comes from the device's current state, or from an earlier sample of the same delivery); a change of `MOVEMENT_THRESHOLD_MPS2` or more moves `device_current_state.last_movement_at`. `device_health` adds a Movement line from it, the map features carry `last_movement_at` and `activity`, and the "Device not moving" template reads `max(activity)` over a window. `shared/domain/reboot.py` does the same for reboots: an uptime lower than the one before it (the device's current state, or an earlier status of the same delivery) is a `device_reset` event with the reset reason of that status, `last_reset_at` moves on the current state, and `device_health` warns on the uptime line for a day. Metrics in seconds are formatted as durations by `formatDuration` in the frontend (`lib/format.ts`); the trend in the map panels is one `MetricTrend` (`components/map/BatteryTrend.tsx`) with a `TrendSpec` per metric (`lib/trend.ts`: the shaped specs for the battery, the movement and the uptime, `trendSpecFor(key, metric, t)` for any numeric metric of the registry with a round axis step from `niceStep`, `decimalsFor`); the "Last status" panel on the map (`StatePanel.tsx`) and the Health card of the device and entity pages (`HealthCard.tsx` with `projectId` and `deviceId`) make every such value a button that unfolds the trend under its row, the registry read once through `hooks/useMetrics.ts` (Tim, 2026-09-15); `MetricTrend` takes `until`, the device's last seen, and ends its period there (a minute past it, capped at now) rather than at now, naming the end date when it is over a day old (decision D207 for the trends).
### Log files and browser syncs

`shared/logfiles.py` parses the one-frame-per-line format (base64 or hex), stores a file in the `device-log-files` bucket and its `device_log_files` row (SHA-256 unique per device, `DuplicateLogFile`), and builds the `log_file.uploaded` message. `services/decoder/protect_decoder/logfiles.py` is the file processing worker: it splits the file, stores every frame with `store_inbound` on the channel's built-in source (the device known up front) and decodes it with `process_source_event`, `LOG_FILE_BATCH_SIZE` frames per transaction, counting frames, malformed frames, records found, new and known (the pipeline's `Outcome` now carries the period, firmware and decoder version); `frames_total` is the count of frames in the file from the split (the stored frames on a re-decode) and `frames_done` is written after every batch, so `LogFilesCard.tsx` draws a bar with the percentage while the status is `processing` (migration 0033); a re-decode reprocesses the stored frames (`provider_metadata.log_file_id`). The API (`routers/log_files.py`) uploads, syncs (`ble-sync`, frames in hex), lists, downloads and re-decodes, and serves the driver's protocol catalogue for the settings editor. Decision D77, ADR 0017, `docs/devices/raw-log-files.md`.

### Adapters and drivers

Contracts in `shared/connectivity/base.py` and `shared/device_drivers/base.py`, registries in `registry.py` next to them (D10, ADR 0011). An adapter may declare `push` (a webhook token is minted for its sources), `acquisition_channel`, `command_connector`, `config_example`, `credentials_schema` and `setup_hint`; `describe_adapter` turns that into the `GET /data-sources/adapters` answer the frontend builds its form from. `tests/shared/test_provider_boundary.py` fails when a provider is named outside `shared/connectivity/adapters/` or anywhere in the frontend. Adapters today: generic HTTP, generic MQTT, ChirpStack (MQTT events, REST management and queue), KPN/ThingPark (HTTP push verified by ThingPark's Token or the bearer, downlink API with a `CorrelationID` the sent report echoes as `queue_ref`; a sent report with `DeliveryStatus` 0 becomes a `log` event with level `ERROR` and the translated failure causes, which fails the command; the best LRR's coordinates on the reception), LORIOT (websocket output, `tx` downlinks), Netmore (export format over HTTP push or MQTT; downlinks through the LoRaWAN Portal or Connect API per the `platform` setting), akenza.io (webhook samples, akenza device id as identity, REST downlinks). Cloudloop (Iridium: Lingo webhook with `?token=` and an address allow-list, `Data/DoSendSbdMessage` commands, `Data/GetThings`). Two built-in channel adapters, `webble` and `log_file`, have no platform: their sources are created by migration 0011 with fixed ids (`builtin`, `SOURCE_ID` in the module), `shared/ingest.py` `builtin_source` and `ensure_channel_identity` find or recreate them and the device's identity on them (its own id); `webble` declares `requires_client` and a command connector that only queues (the browser executes). Adapters produce `InboundMessage` (with gateway receptions and identity attributes for links, and `device_id` when the caller already knows the device, as browser syncs and uploads do) and never decode payloads; drivers produce `DecodedRecords` and never read provider fields. For LoRaWAN uplinks the decoder extracts the application frame (`frame` bytes and `f_port`) from the stored event and drivers work on that; on the `webble`, `log_file` and `iridium` channels the frame is the raw delivery (`data_hex`, `raw_frame` in `device_drivers/base.py`) and `SourceEventData.acquisition_channel` tells the driver how to read it (the OpenCollar driver: port byte in front over BLE and in files, a stored-record stream over Iridium); network-level events (`join`, `status`, `downlink_ack`, `downlink_transmitted`, `log`) need no driver: `status` yields `battery_level` and `link_margin` measurements, the rest update connectivity state. A driver declares `decodable_event_types` (default `uplink`). Skeletons to copy are in `examples/`. Documentation: `docs/integrations/adapter-interface.md`, `docs/devices/driver-interface.md`, `docs/integrations/chirpstack/`.

### OpenCollar firmware layouts

The OpenCollar driver has `LAYOUTS`, one per firmware range named after the reference decoder (`fw7.2.0`, `fw6.15.1`, `fw6.11.2`, `fw6.5.0`, `fw4.4.3`): the ports with message id and fixed length, whether status feature bit 2 is the RF scanner, the CMDQ record length. `layout_for(version)` selects it from `SourceEventData.firmware_version` (the device's, written by the decoder from status messages) and a flash log stream switches layout after a status record; `records.decoder_version` names the layout. Reference decoders are vendored under `tests/fixtures/payloads/opencollar/decoders/`, `scripts/opencollar_golden.py` runs every recorded frame through them (node) into `golden.json`, and `tests/shared/test_opencollar_golden.py` compares (D100, ADR 0021).

### ChirpStack locally

`docker compose --profile chirpstack up -d` starts ChirpStack 4.19, its gateway bridge, Mosquitto, Postgres and Redis and the REST API. `scripts/dev.sh chirpstack-bootstrap --protect-email ... --protect-password ...` mints an API key (a row in ChirpStack's `api_key` table plus a JWT signed with `CHIRPSTACK_API_SECRET`), creates tenant, application, device profile, simulated gateway and device, and registers the data source in Protect. `scripts/dev.sh simulate --application-id <id>` publishes uplinks as ChirpStack integration events on the broker. The gateway bridge config has the broker host written literally because the bridge does not expand environment variables.

### Live map, traffic, traces, health

- `GET /projects/{id}/map/current` returns entity current state as GeoJSON within a viewport and limit and says whether the client should switch to tiles (`use_tiles` above 2,000 entities); `GET /projects/{id}/map/tiles/{z}/{x}/{y}.mvt` serves the same as Mapbox vector tiles from `ST_AsMVT` (layer `entities`).
- `GET /projects/{id}/tracks?entity_id|device_id&from&to&max_points` returns a LineString with one time per vertex, decimated with a uniform step so at most `max_points` come back; `total_points` and `step` tell the client how much was skipped.
- `GET /projects/{id}/traffic` lists source events of the project's devices with the acquisition channel and ingestion method, the frame length and the delivery time (Bluetooth sync, file upload or satellite delivery), and for LoRaWAN rows port, frame counter, spreading factor, best RSSI and SNR, gateway receptions, plus the processing status; `include_payload=true` adds the raw payload. The frontend renders every traffic list with `components/network/TrafficTable.tsx`, whose Details column follows the row's channel (phase 15).
- `GET /projects/{id}/traces` searches traces by device, DevEUI, data source, status, error code and time; `GET /traces/{id}` has the steps.
- `GET /system/health` (server admin) reports workers with heartbeat, lag and dead letters, events per minute, failures, unknown identities and per data source counts.
- `WS /api/v1/ws/projects/{id}?token=...` streams `position.created`, `device.state_changed`, `event.created` and `alert.created` for the project. One reader per API process tails the streams with `XREAD` and fans out; there is no consumer group, so every API instance sees every message (`protect_api/realtime.py`).

### Errors

- Application errors use the stable codes from architecture section 26.5 (`PAYLOAD_DECODE_FAILED`, `DEVICE_NOT_FOUND`, `TIMESTAMP_INVALID`, ...). Each has severity, retryable and user-actionable flags.
- Crash early in development. Unexpected states raise; they are not logged and skipped.

### Naming

- Containers `protect-<service>`, network `protect-network`, database `smartparks_protect`.
- Python packages use snake_case, adapter keys and driver keys are lowercase identifiers (`chirpstack`, `kpn_thingpark`, `loriot`, `opencollar`).
- Metric keys are lowercase snake_case with the canonical unit in the registry (`battery_voltage` in V, `temperature` in °C).
- Icon keys are dotted (`wildlife.wolf`, `device.lora_gateway`).

## Local development

Requirements: Docker with Compose v2, [uv](https://docs.astral.sh/uv/), Node 24. On WSL2, enable the Docker Desktop WSL integration for the distro.

```bash
cp .env.example .env
scripts/dev.sh hooks                      # git config core.hooksPath .githooks
scripts/dev.sh up                         # docker compose up -d --build: postgres, redis, minio, migrate, api, ingest, decoder, export, rules, automation, integration, mcp, frontend
scripts/dev.sh bootstrap-admin you@example.org   # invitation link for the first server admin (once)
docker compose --profile chirpstack up -d # adds a local ChirpStack (web UI and gRPC on :8080, MQTT on :1883)
uv sync --all-groups                      # python environment for tests, linters and docs
cd services/frontend && npm ci            # frontend dependencies
```

Daily commands through `scripts/dev.sh`: `up`, `down`, `logs [service]`, `migrate`, `revision`, `bootstrap-admin`, `openapi`, `test`, `lint`, `format`, `docs`, `sweep`, `hooks`, plus `chirpstack-bootstrap` and `simulate` for the local ChirpStack.

Ports (all bound to localhost except the frontend): API 8000, MCP 8001, frontend 3000, Postgres 5432, Redis 6379, MinIO 9000 and console 9001. `/api/docs` is the OpenAPI UI, `/api/health` reports database, Redis and MinIO, `/api/version` reports the version and commit.

The frontend dev server (`npm run dev` in `services/frontend`) runs on :5173 and proxies `/api` and `/ws` to `VITE_PROXY_TARGET` from the root `.env`. The frontend reads the root `.env`, there is no second env file.

### Python workspace

One `uv` workspace: `shared/` and `services/*` (the frontend is excluded). Every `pyproject.toml` pins exact versions; `uv.lock` is committed and CI installs with `--frozen`. Add a dependency with `uv add --package smartparks-protect-shared <name>`, then pin it to the resolved version.

`shared/shared/` today: `config.py` (pydantic-settings, `get_settings()` cached, raises on missing values), `database.py` (one async engine per process, `get_session` dependency, `session_scope` for workers), `logger.py` (JSON or text, `request_id` and `trace_id` context variables, `get_logger(name).info("msg", key=value)`), `version.py` (reads `VERSION`, falls back to `v0.0.0-dev`).

`services/api/protect_api/`: `main.py` builds the app, `middleware.py` sets the request id (inbound `X-Request-ID` is kept), `health.py` runs the three dependency checks concurrently with a 3 second timeout each and answers 503 when one fails.

### Frontend

WebBLE (`src/lib/opencollar-ble.ts`, decision D76) implements the OpenCollar BLE protocol over an injected transport so `opencollar-ble.test.ts` runs without hardware; `stores/webble.ts` keeps one session per device in the tab, `hooks/useWebBle.ts` syncs received frames to `POST /devices/{id}/log-files/ble-sync`, `components/devices/WebBleCard.tsx` is the card and `DeviceControl` executes commands whose route is the browser. Web Bluetooth needs Chrome or Edge over HTTPS; the DOM lib does not type it, so the subset used is declared structurally in the protocol module.

`pages/auth/AuthShell.tsx` frames the sign-in, registration and password pages: the card with a dark wash over one of the photographs in `BACKGROUNDS`, picked at random each time the frame mounts (`public/auth-background-1.webp` to `-4.webp`: Smart Parks landscapes photographed by Tim van Dam and used with his permission, at most 2560 px, blurred in the file with a Gaussian radius of 7 at that width so the browser blurs nothing), the way AddaxAI Connect does it; the files are served unhashed from `public/`, so a server may replace them without a rebuild. `npm run build` is `tsc -b && vite build`: a type error fails the build and the Docker image. `npm run lint` is ESLint, `npm run test` is Vitest with Testing Library, `npm run sweep` is the screenshot sweep (see below). shadcn/ui components are added with `npx shadcn@latest add <name>` and land in `src/components/ui/`, which ESLint ignores. Brand colours and semantic tokens are CSS variables in `src/index.css`; the brand in the app is `assets/brand/logo-landscape.webp` shown as an `<img>`; the `currentColor` SVGs in `src/assets/brand/` are kept for the favicon and for a component that needs a tinted mark (`vite-plugin-svgr` turns `…svg?react` into a component). Rules are in `services/frontend/FRONTEND_CONVENTIONS.md`.

How the app is put together:

- `src/api/client.ts`: the one fetch wrapper. Attaches the bearer token, turns errors into `ApiError`, calls `useAuthStore.expire()` on a 401 so the router shows the login page with a return path. No `window.location` anywhere.
- `src/api/schema.d.ts` is generated from the API's OpenAPI document with `scripts/dev.sh openapi` (also `npm run generate:api` after the JSON is dumped); `src/api/types.ts` aliases the schemas the pages use. CI fails when the committed schema is stale.
- `src/hooks/useNow.ts` is the clock behind live "ago" values: a page calls `useNow()` and passes the result to `formatAgo(value, now)`, so the text moves every 30 seconds without refetching.
- `src/api/queryKeys.ts` is the only place query keys are defined; `useMutationToast` invalidates by these keys and toasts the outcome.
- Stores: `useAuthStore` (token persisted in localStorage, user, status), `useProjectStore` (last opened project), `stores/layout.ts` (`sidebarHidden`, persisted, behind the collapse and expand buttons of `AppLayout.tsx`), `stores/icons.ts` (the icon registry) and `stores/webble.ts` (the Bluetooth session). Filters and selection live in the URL. Interface preferences are one document per user: `hooks/usePreference.ts` reads a key from the signed-in user and saves the whole document to `PATCH /api/v1/users/me` (`users.preferences`, bounded at 32 KB) 800 ms after the last change; it is the store behind `track_length`, `heat_settings`, `terrain`, `feed_seen`, the layer choices per project and the reveal notes.
- Search: `routers/search.py` (decision D99) is one bounded endpoint over everything the caller may see (entities, devices, features, gateways, projects and pages); `components/layout/CommandPalette.tsx` is the Ctrl+K box over it, with the sidebar's search field as the trigger.
- Groups: `routers/groups.py`, the `Group` model, `pages/project/GroupsPage.tsx` and `hooks/useGroups.ts` are entity groups as nested folders (ADR 0020), the first level of the layers panel and a device filter.
- Reads: `routers/data.py` is the canonical read API (positions, measurements, source events, traces per project and per object) the map, the records canvas and the explorer build on; `shared/metrics/seeds.py` seeds the metric registry (keys, units, categories) behind `/attention/metrics` and the explorer's labels; `shared/domain/links.py` builds the "Open in <platform>" links from a data source's link templates and an identity's attributes, so no provider name reaches the interface (`resolve_links`, used by the device views and `gateway_read`).
- Routing in `src/App.tsx`: `/projects/:projectId/...` behind `RequireAuth`, `/admin/...` behind `RequireServerAdmin`; pages are lazy loaded. Sidebar sections in `src/components/layout/navigation.ts`, items without a route show the phase they arrive in.
- Icons: `src/assets/icons/icon-registry.json` maps stable keys (`wildlife.rhino`) to SVGs with fallback chains and licence data (`src/components/icons/registry.ts`); `Icon` renders inline, `markers.ts` renders marker images for MapLibre with the family shape (round entity, square infrastructure, diamond event) and a state colour. About 350 of the icons are EarthRanger's (decision D165): `scripts/import_earthranger_icons.py` holds the table of keys and source files at a pinned commit of PADAS/er-server (Apache 2.0, the licence next to the files as `LICENSE-EarthRanger`), downloads them and normalises each SVG (white halo shapes dropped when dark ones exist, styles, classes, titles and fills removed, strokes to `currentColor`, the viewBox padded square, coordinates rounded to two decimals with a separator kept between numbers that touched), then rewrites the registry entries one per line; entries the table does not name are ours and stay. `registry.test.ts` checks every entry draws through its own file, every fallback exists and every SVG has that shape. `IconPicker.tsx` is the searchable choice (a popover with `Command`, grouped by category, the search over label, key and aliases) the catalogue dialogs use; `eventIcon` in `lib/rules.ts` and `ICON_BY_TYPE` in `routers/events.py` are the same table from event type to icon key.
- Access (phase 27, decisions D185 to D188, ADR 0030): `shared/permissions.py` holds the keys, `AREAS` (nine groups for the role editor), the four built-in roles (`ROLE_PERMISSIONS`, each a superset of the one before) and `permissions_for(role, server_admin=, custom=)`, where `custom` is a custom role's key list with `project:read` always added (`normalise_permissions` refuses an unknown key). `project_roles` rows (`ProjectRole`, migration 0026) are custom roles; a membership names one in `role_id` (wins over the built-in `role`) and may carry a `scope` document (`groups`, `entities`, `devices`); invitations carry both and registration copies them. `protect_api/visibility.py` resolves a scope per request into a `Visibility` (group ids with everything below, entity ids from the groups and the single entities, device ids from the single devices and the devices tracking a visible entity today) with the SQL filters `entities(column)`, `devices(column)` and `rows(entity_column, device_column)`, the checks `entity_visible` and `device_visible`, and `narrow_entities` and `narrow_devices` for a caller's own selection; `group_and_subgroups` lives there now. `deps.membership_access` reads the row once and `ProjectContext` and `ScopeContext` carry `visibility`; every project read applies it (entities, groups, devices, the map's current state, device layer, positions, tracks, heat and network locations, `positions/at` and the device state, events and alerts, records, analysis owners, exports through `_in_scope` on the job's parameters, search, coverage's SQL, `_scope_device_ids` for traffic, gateways and connectivity, `_device_visible` for source events and traces, `_control_permissions` for commands); `visibility_for(session, user, project_id)` serves the device routes outside the project prefix. Endpoints: `GET /permissions` (the catalogue: areas and built-in sets), `/projects/{id}/roles` CRUD (`members:write` to write, a role in use cannot be deleted), members and invitations with `role_id` and `scope`, and `ProjectWithRole.permissions` and `scope_limited` on the project list. Features, saved views and dashboards have their own keys (`features:write`, `views:write`, `dashboards:write`). Frontend: `lib/permissions.ts` (the keys, the labels and hints per key and area, the role names), `usePermissions(projectId)` in `hooks/useProjects.ts` returning `can(key)` from the project list, the navigation model's `permission` per item and section, `RequireProjectPermission` around the project admin routes, and `pages/project/MembersPage.tsx` with the Members, Roles and Invitations tabs, the `ScopeDialog` (groups tree, entities, devices) and the `RoleDialog` (checkboxes by area). Tests: `tests/shared/test_permissions.py`, `tests/api/test_roles_and_scope.py`, and the access matrix's viewer allow-list. The server admin's user page (D189): `GET /admin/users/{id}` answers `UserAdminDetail` (the account plus `memberships`, each with the membership id the members endpoints take, the project, the role, the keys and the scope), `PATCH /admin/users/{id}` takes `email` and `full_name` besides the flags, `POST /admin/users/{id}/password-reset` sends the forgot-password mail through the user manager; `pages/admin/UserPage.tsx` edits memberships through the project's members endpoints and shares the scope picker and the role helpers with the members page (`components/members/scope.tsx`, `roles.ts`, `tree.ts`); `tests/api/test_admin_users.py`. A server admin's invitation (D190): `POST /admin/invitations` takes `server_admin` and `memberships` (project, role, `role_id`, `scope`, each checked with the members endpoints' validators) and answers `ServerInvitationResult`: an `InvitationRead` whose `memberships` list (migration 0027, `invitations.memberships` JSONB) registration turns into membership rows, or for an address with an account `user_id` and `added_projects` with the memberships created at once; the mail and `GET /auth/invitation` name every project (`project_names`). The Users page's form (`pages/admin/UsersPage.tsx`) uses `components/members/RoleSelect.tsx` and the scope picker per row. `DELETE /admin/users/{id}` (D191) deletes the row and relies on the foreign keys (memberships, OAuth codes and tokens and MCP pending actions cascade; every other reference to users sets null), after the audit row `user.deleted`; it refuses the caller's own account and the last active server admin.
- Layout: the top bar in `AppLayout.tsx` shows on every screen size (decision D182) with the landscape logo and the product name on the left and, on the right, `ThemeSwitch.tsx` and `HealthDot.tsx`; it is 41 px on a phone (32 px buttons, a 24 px logo, the name on one line at `text-sm`, truncated rather than wrapped) and 53 px from the `sm` breakpoint up; the navigation column (`Sidebar.tsx`) keeps only its collapse button above the project switcher. Theme (decision D183): `lib/theme.ts` (`resolveTheme`, `nextTheme`, `applyTheme` toggling the `dark` class, the colour scheme and the `theme-color` meta, the browser mirror `protect-theme`, tested), `hooks/useTheme.ts` (the preference `theme` with the mirror as fallback, `matchMedia` for system through `useSyncExternalStore`, applied in an effect), an inline script in `index.html` that applies the mirror before the first paint, `@custom-variant dark` and the `.dark` token block in `index.css` (the same tokens on dark green-grey surfaces, the brand green lightened as the primary), MapLibre's attribution and scale boxes restyled under `.dark`, and `lib/chartStyle.ts` `chartTheme(dark)` for the charts' text, grid and tooltip colours. Base maps (decision D184): `basemap.ts` has `auto` ("Follows the theme", the default of `loadBasemap`) and `dark` (labelled Night: OpenFreeMap's `fiord`, a dark blue-grey; its `dark` style is near black and was too black for Tim); `basemapStyle(key, maps, darkTheme)` resolves `auto` to Night at night and Light by day and every map passes `useTheme().resolved`; the mini map rebuilds on a theme change. `components/layout/HealthDot.tsx` (decision D181) is the system health dot every account sees in the top bar; it polls `GET /system/status` (`system_status` in `routers/network.py`: the workers past `heartbeat_stale_minutes` and the system alerts open over `SYSTEM_ALERT_GRACE`, for any signed-in account) every minute and `lib/health.ts` (`dotLevel`, tested) turns the summary and the query's `failureCount` into ok, degraded, down or unknown, red only after `DOWN_AFTER_FAILURES` polls in a row without an answer. The landscape logo (`assets/brand/logo-landscape.webp`) is the brand everywhere a page shows it: the sidebar header beside the product name, the phone header, and the sign-in card; `logo-mark.svg` survives as the favicon and `logo-wide.svg` and `logo-stacked.svg` are imported by nothing today.
- Map: `useMap` creates one MapLibre map with OpenFreeMap styles (D37) and sends the bearer token on requests to our own origin; `layers.ts` owns the entity, track and feature sources: `ensure*Layers` adds a source and its layers once, `bindEntityClicks` and `bindEventClicks` register the click handlers and return the function that removes them. Bind clicks in an effect and unbind in its cleanup, never inside `ensure*`: the map and the page outlive a switch to another project (only the route parameter changes), and `setSearchParams` resolves against the pathname it was created with, so a handler bound once keeps writing to the first project's URL. Live updates come from `useProjectStream` over the WebSocket and patch the current-state query cache: a `position.created` moves the entity's and the device's feature when it is at least as new as the one shown (`isNewer` in `components/map/live.ts`, so a raw log decoded now moves nothing back), eases the map to it while the selection is followed (decision D210: `followPausedFor` state, a `dragstart` by hand pauses it for that selection, the `FollowButton` in the entity and device panels' footer resumes it), and refreshes the tracks and the heatmaps of the objects that report through `createSettler` (one invalidation five seconds after a burst). Phones: `hooks/useMediaQuery.ts` (`useIsPhone`, below the `sm` breakpoint) is the one way to branch on width in code; `DataTable` takes `defaultHiddenSmall` for columns a phone hides by default; the shadcn `Card` carries `min-w-0` (noted in the file) so a table inside a card scrolls within it instead of widening the page. The screenshot sweep takes `SWEEP_PROJECT` and adds the map with a selection and tracks to its routes. `lib/staleChunk.ts` (decision D130) listens for Vite's `vite:preloadError`, the event a failed dynamic import of a page chunk raises, and reloads the page once (a `sessionStorage` stamp stops a loop within 30 s): after a deploy the hashed chunk names of the old build are gone, so a tab opened before the deploy went blank on its first lazy page. `components/map/maplibre.ts` sets MapLibre's worker URL once and every map imports MapLibre through it (a map built from the package directly renders nothing in the Vite build). A "show on map" link (`?entity=` or `?device=` on arrival) centres the map on the object at zoom 14 or the current zoom if closer, in place of the fit over the project (Tim, 2026-09-15); the fit effect does it once per project visit. The device and entity pages' small map and Recent positions card show the 30 days up to the object's last known record (`lib/positionsWindow.ts`, anchored on the device's `last_seen_at` or the entity's live position time from the current state, which leaves clock-ahead rows out, capped at now), not the 30 days before now, so a device silent for a year still shows its last month (decision D207). `MiniMap.tsx` (decision D124) is the still map of the entity and device pages: its own MapLibre map without gestures or controls, the trail and the newest position from `miniMap.ts` (`miniMapGeometry`, tested), fitted to the bounds, wrapped in a link to the live map; the attribution is a text line since the map itself is the link. The device layer (decision D111): `GET /projects/{id}/map/devices` lists every device with a project assignment valid now with its own current-state position, health and the entity it tracks; `ensureDeviceLayers` draws them as square markers under the entities, clustered on their own source (white ring clusters translated 10 px down and right of the entity clusters, markers offset 16 px so a device shows beside its animal), `layerChoices.shown_devices` is a shown set (everything off until switched on), `?device=` selects a device (never together with `?entity=`), `?device_tracks=` lists the device tracks, drawn dashed from the same track source with `kind: "device"`. Tracks: `?tracks=` lists the entities and `?track=` the length for all of them, hours or `assigned` (`components/map/trackLength.ts` parses, clamps to 1 hour to 90 days, and computes each entity's `from`, which for `assigned` is the feature's `assigned_since` from the map's current-state endpoint); `TrackSettings.tsx` is the Tracks card and the settings panel; the last length is the per-user preference `track_length` (decision D109). Panels (phase 19, decision D135): `components/map/MapObjectPanel.tsx` is the one panel shell (`MapPanel` with `PanelRow`) and the entity and device bodies; `PointPanel.tsx` reads `GET /projects/{id}/positions/at?entity_id|device_id&time=` (`position_at` in `routers/map.py`: the position at that effective time through `at_time` in `shared/curation/effective.py`, an index-friendly equality, plus the device's measurements of the same moment with their metric labels) and links to the Data tabs with `?at=`, which `hooks/useAt.ts` turns into a 12 hour window around that time on the entity and device pages; `GatewayPanel.tsx` reads the project gateway detail and toggles `toggleOnlyGateway` (`layerChoices.ts`), which sets `coverage_gateway` on the layer choices; `coverageGatewayIds` turns that, or the ticked gateways, into the coverage query's filter and the Coverage tab names the narrowing with a way back (a field rather than a hidden list: the preference document is bounded at 32 KB and the all scope's entry on the dev server already takes 30 KB). Track point features carry `owner_id`, `kind`, `time` and `key` (`trackPointKey`); `track-point-selected` is a filtered layer over the same source (`setSelectedTrackPoint`); `bindTrackPointClicks` and `bindGatewayClicks` are bound in the same effect as the other clicks. The selection is one of `?entity=`, `?device=`, `?gateway=` or `?point=<owner>,<time>` (`selectOnly` clears the others); a `device:` prefix on the owner names a device's own fix, the plain form an entity's or, while its track is on, a device's, and the position time in the entity and device panels (`onOpenPosition` on `HealthRows`) selects the newest fix that way; `?state=<device>` opens `StatePanel.tsx` over `GET /projects/{id}/devices/{device_id}/state` (`device_state` in `routers/map.py`: the driver's health lines from `device_health`, the raw `latest_state` document, and the source event of the `DeviceStateHistory` row at `latest_state_time`; 404 before any status or outside the scope), from the "Last status" time of the same panels (`onOpenState`); `CopyPositionButton` in `MapObjectPanel.tsx` puts "latitude, longitude" on the clipboard from the entity, device and fix panels. The fit on a project visit takes entities and devices together through `fit.ts` (`boundsOf`) once both reads have settled, and keeps the view when nothing has a position. The control strip (decision D137): `useMap` adds an empty MapLibre control (`StripHost`) to the top right stack and returns its element as `stripHost`; the map page renders `components/map/ControlStrip.tsx` into it with `createPortal`, a list of `StripItem`s (a button with icon, label, active, badge and a click, or a menu with options), and a second host at the bottom right holds zoom in, zoom out, reset north and Locate (decision D173), so every button on the map is one of ours at one size; MapLibre's navigation and geolocate controls are gone. `components/map/locate.ts` is the Locate toggle: `navigator.geolocation.watchPosition`, a dot and an accuracy ring (`circleRing` from `lib/geodesy.ts`) on their own source, `easeTo` on every fix while following, a `dragstart` ends the following and keeps the dot, `stop` removes everything; the status (`off`, `waiting`, `following`, `tracking`, `denied`) drives the button's label. The Layers button is the one `emphasis` item (filled when idle, decision D174), rendered top left (beside the events count only, the entity and device count went on 2026-09-12; its own count badge went on 2026-09-16, the number said nothing to a reader); the Tracks and Heatmaps items exist only while a track or a heatmap is on. The Feed (decisions D177 to D180) sits under Layers: `lib/feed.ts` (`unreadCount`, `newestCreatedAt`, `feedPosition`, tested) over the newest 100 of `GET /projects/{id}/events` (the `events` query key, invalidated by the stream's `event.created` and `alert.created`), the preference `feed_seen` as a map of project scope to the newest creation time seen (the first visit sets it at the newest item, an open panel keeps it there), `?feed=1` for the panel like `?layers=1` (opening one closes the other), `components/map/FeedPanel.tsx` for the list with Acknowledge on open alerts, a row easing the map to the event and setting `?event=` for the detail dialog, and a `toast.warning` with a Show action on `alert.created`. Everything else opens in the right column: an absolutely placed flex column (`right-14` under the strip on desktop, a bottom sheet on a phone) holding the Tracks card, the track settings, the layers panel (`flex-1`, it no longer positions itself) and the object panel (`MapPanel`, `max-h-[45vh]`); the top left holds the Layers and Feed buttons and the events-of-24-hours badge, nothing else. Every tab of the layers panel shares one footer: "N of M shown", Fold all, and one Show all or Hide all toggle (`showAll*` and `hideAll*` in `layerChoices.ts`). The layers panel's state is `?layers=1`. The heatmap (decision D138): `GET /projects/{id}/map/heat` (`heat_points` in `routers/map.py`: the newest `MAX_HEAT_POINTS` positions in the viewport and window for up to 500 entity and 500 device ids, or the whole scope without ids; bounded twice, since the dev server has 2,082 devices seen in a week: the scan runs per device through the device and time index of the compressed chunks, a bounding-box scan over every chunk took 21 s, and reads at most `MAX_HEAT_DEVICES` devices, the most recently seen first, of those seen in the window and, in a project, assigned to it then; the answer carries `capped`, `devices_scanned` and `devices_total` and the card says so) feeds one MapLibre `heatmap` layer (`ensureHeatLayer`, `setHeatPoints`, `setHeatPaint` in `layers.ts`) under the marker layers; `components/map/heat.ts` holds the settings (`radius_m`, `sensitivity`, `hours`), their URL form, `radiusExpression` (metres to pixels per zoom at the map's latitude as interpolation stops, clamped to 2 to 200 px) and `intensityFor`; `HeatSettings.tsx` is the card and the settings panel; heatmaps are per entity and device like tracks (Tim's correction of 2026-09-09): `?heat=` and `?device_heat=` list them, the layers panel rows and the entity and device panels carry a toggle next to the track's, one request carries both lists, refetched per settled viewport with the previous data kept; the last settings are the preference `heat_settings`. Drawing (decisions D139, D171, D172): `components/map/draw.ts` wraps terra-draw with its MapLibre adapter in one `DrawSession` (point, line, polygon and circle modes in the brand colours: line and polygon carry `showCoordinatePoints` so every vertex is a marker from the first tap and `editable` so a finished shape's vertices drag, the circle is 64 segments with `drawInteraction: "click-move-or-drag"` so a phone draws it by dragging out from the centre and it is dragged whole in select mode, a select mode for midpoints and deletion, Escape and Enter as the key events), `begin(kind)`, `state()`, `clear()`, `destroy()`; the session reports a `DrawState` on every change: `geometry` (the finished shape), `live` (the shape in progress, what the bar measures) and `circle` (centre and radius from terra-draw's `radiusKilometers`, absent until the pointer moved). The map page keeps a session while a tool is on (`tool` is transient state, never in the URL), `DrawBar.tsx` is the bar with the live length, area or radius and the `SaveFeatureDialog`; a circle is saved as a 64-point polygon (`circleRing`) with `shape: circle`, `centre` and `radius_m` in the feature's attributes, so rules and exports see a polygon and `FeaturePanel.tsx` names the radius; `featureTools.ts` says what a geometry can become and which rule template a feature starts, and the Features page's dialog draws with the same session. Measuring (decision D141): `lib/geodesy.ts` (haversine distance, spherical ring area after Chamberlain and Duquette, formatted lengths and areas), tested against known values. Features on the map: `features-point` draws sites, `bindFeatureClicks` selects `?feature=`, `FeaturePanel.tsx` shows the measure and links to `/rules?new=<template>&feature=<id>`, which the rules page turns into an open editor (`initialTemplate`, `initialFeatureId` on `RuleEditor`: the template's first spatial or near condition takes that feature). The proximity condition (decision D140): `NearCondition` in `shared/rules/schema.py` (`meters` plus feature ids, a feature type or entity ids), evaluated in `evaluator.py` with `metres_between` (haversine) for entities and `metres_to_geometry` (a local planar projection around the point) for features, the nearest distance as the condition's value; `DataAccess.entity_points` (the current state live, the newest position before the sample time in replay) in `data.py`; the `near_site` template; `near` left the reserved types. Imagery and terrain (decision D141): the server setting `MAPTILER_KEY` reaches the browser through `GET /api/v1/map/config` (`routers/map_config.py`, any account; the key is public by nature and restricted by referrer at MapTiler) because the frontend image is built once for every server, so a `VITE_` variable would need a rebuild per server; `hooks/useMapConfig.ts` reads it once per session. `basemap.ts` offers the free OpenFreeMap styles and, with a key, MapTiler's `hybrid` style as Satellite (`basemapsFor`, `basemapStyle`, a saved choice the server does not offer falls back to Light); `useMap` takes a style URL. `terrain.ts` adds MapTiler's `terrain-rgb-v2` TileJSON as a raster DEM source with a hillshade layer under the data layers and calls `setTerrain` (`TERRAIN_EXAGGERATION` 1.3); a style change drops sources, so the map page applies it again whenever `ready` flips; the switch is the preference `terrain` and eases the pitch to `TERRAIN_PITCH`; our own Reset north button in the bottom right strip puts bearing and pitch back to zero. A link arrival reveals the object (decision D136): `revealEntity`, `revealDevice`, `revealGateway` and `revealFeature` switch the object and the layers above it on in the preference, once per arrival, and `?revealed=<key>` carries the note the panel shows.

Screenshot sweep: `npm run sweep` (Playwright, routes derived from `src/App.tsx`) logs in with `SWEEP_EMAIL` and `SWEEP_PASSWORD`, opens every route at 390, 768 and 1440 px, flags console errors and horizontal overflow and writes `ui-sweep-output/`. Chromium needs a few system libraries (`libnspr4 libnss3 libasound2t64` on Ubuntu 24.04); without them run the sweep in the Playwright image from `services/frontend`: `docker run --rm --network host --user "$(id -u):$(id -g)" -v "$PWD:/work" -w /work -e SWEEP_EMAIL=... -e SWEEP_PASSWORD=... mcr.microsoft.com/playwright:v1.58.0-noble node scripts/ui-sweep.mjs`. The `--user` flag keeps the output owned by you.

## Rules, events, alerts

`shared/rules/schema.py` is the rule document (decision D9, ADR 0012): Pydantic models, `RuleDocument.reserved_types()` says what the evaluator cannot run, `json_schema()` feeds the frontend builder. `shared/rules/evaluator.py` is the stateful evaluator: `evaluate(doc, subject, sample, state, data)` returns a `Verdict`; it is pure apart from the `DataAccess` protocol, implemented for SQL in `shared/rules/data.py` (live mode reads the current-state tables, historical mode derives everything from rows before the sample time). Firing is edge-triggered with an optional cooldown reminder; `SubjectState` (active, holding since, last fired, inside geofences) is stored per rule and subject in `rule_state`. `shared/rules/events.py` creates events and alerts the same way for rules, system checks and later the API (`create_event`, `close_alert`, `event_messages`); `shared/rules/replay.py` replays canonical rows through the evaluator with in-memory state, bounded.

`services/rules/protect_rules/engine.py`: `RuleCache` re-reads enabled rules every `RULES_RELOAD_SECONDS`; `handle_position`, `handle_measurements` and `handle_state` build a `Sample` and run every rule in scope, each in its own transaction (`run_rules`), so one broken rule lands on `rules.last_error` plus a failed trace and the others continue. A rule that fires writes a compact trace (rule matched, conditions evaluated, event created) and publishes `event.created` and `alert.created` after the commit; silent evaluations write nothing. `Scheduler.tick()` runs schedule rules over the entities of the project (at most 5,000) every `every_seconds`. `system_checks.py` opens one system alert per stale worker, dead-letter stream and lagging consumer group and resolves it when the finding clears; system events have `project_id` null and are visible to server admins only.

Events and alerts API: `routers/events.py` (project lists newest first with the time of the last item as cursor, detail with deliveries, `map/events` GeoJSON for the last hours, acknowledge and resolve under `alerts:write`, which viewers hold) and the `/admin` variants for system events. Rules API: `routers/rules.py` (templates, schema, versions as immutable rows, `PUT /document` makes a new version, `POST /{id}/test` and `POST /test-document` replay).


Every event type the system raises has a plain-words explanation in `shared/domain/explanations.py` (`EVENTS` per type, `ERROR_FLAGS` per OpenCollar status flag, `RESET_REASONS`); `event_read` adds it as `explanation` and the event dialog shows it as "What this means". A new event type or flag gets its entry there; `tests/shared/test_explanations.py` holds every template's type to it.
## Automations and notifications

`services/automation/protect_automation/actions.py`: `handle_event` loads the matching automations of the event's scope, and for every action gets or creates the `ActionDelivery` row keyed on (event, automation, action index), so a re-delivered bus message runs only the actions that have not succeeded. Stale events (older than `max_event_age_seconds`) are recorded as skipped. `Skipped`, `PermanentFailure` and `TransientFailure` from `shared/notifications/dispatch.py` decide the delivery status; a transient failure makes the handler raise a retryable `ApplicationError` after the commit, so the bus re-delivers with backoff. Webhooks are signed with `X-Protect-Signature: sha256=<hmac>` when the action has a secret. `telegram_poller.py` long-polls the bot (offset in Redis `telegram:update_offset`) and links `/start <code>` messages to targets.

`shared/notifications/`: `render.py` (Jinja templates for event and test messages, the link back), `email.py` (SMTP with the development guard: non-production servers mail only `DEV_NOTIFY_EMAILS`, everything else is logged and the delivery is skipped, unless `MAIL_DELIVER_TO_ALL` lifts the guard, as on the dev server since 2026-09-13; `allowed_recipient` returns the reason, which an invitation answers as `mail_reason` with a `registration_link` to share by hand), `telegram.py` (Bot API over httpx), `dispatch.py` (`deliver_to_target`, shared by the automation service and the API test send). The API mailer for invitations and password resets uses the same sender.

Targets, automations and deliveries API: `routers/automations.py`, one implementation for the project scope and the `/admin` scope (project null). `automations:write` is project admin only.

## Device control

`shared/control/actions.py` is the action contract (ADR 0013): a driver declares `control_actions`, each a `ControlAction` with a Pydantic parameter model, permission, confirmation policy, required capability, `encode` and optional `interpret`. `shared/control/commands.py` is the one command path: `request_command` creates the row, encodes, selects the route (`select_route`: an enabled data source holding an identity of the device whose adapter has a `command_connector` and the capability; most recently seen identity first; a `route_source_id` pins the route and adapters with `requires_client`, the browser, are skipped unless pinned, decision D79; `candidate_routes` lists every option for the control dialog), submits through the connector (the options carry the identity type and attributes, which Cloudloop needs for the thing id) and records every stage as a `CommandExecution`; `apply_provider_signal` (called by the decoder for `downlink_transmitted`, `downlink_ack` and `log` events, matched on `provider_ref`) and `interpret_device_records` (called by the decoder after decoding an uplink of a device with pending commands) move the lifecycle; `expire_commands` runs in the rules service ticker. Statuses only move forward (`RANK`); final states are `confirmed_by_device`, `failed`, `expired`. A refused submission is a failed command with the reason, not an exception to the caller.

The ChirpStack connector (`ChirpStackCommands`) posts to `/api/devices/{dev_eui}/queue` and returns the queue item id as `provider_ref` with statuses `accepted_by_network` and `queued`; `queue` and `flush` read and clear the platform queue. Adapters without a `command_connector` method cannot send commands, which the actions endpoint reports as the reason.

API in `routers/control.py`: permissions are judged in the device's current project (`_control_permissions`), confirmation policies other than `none` need `confirmed: true`, everything is audited. `GET /devices/{id}/routes` lists the routes, `POST /devices/{id}/routes/webble` registers the browser (the device's identity on the built-in WebBLE source), `CommandCreate.route_data_source_id` pins one, and `POST /commands/{id}/browser-result` is how the browser reports that it wrote (or could not write) a WebBLE command; the device's answer comes through the synced frames and `interpret_device_records`, as for any uplink. The automation action `command` calls `request_command` with the automation as actor and publishes `command.updated` after the commit.

## Integrations

`shared/integrations/base.py` is the outbound connector contract: `render(integration, item)` (pure) and `deliver(integration, item, payload)`, plus `test`. `DeliveryItem` is the canonical object with the names around it; `load_item` in `shared/integrations/deliveries.py` builds it from a delivery row (position, event or measurement, entity and type, device, project, data source, the event's link, and for an event without a point the entity's latest position as a fallback). Connectors live in `shared/integrations/connectors/` (`gundi` and `earthranger` for EarthRanger, `wildlifenl`, `ferustracker`, `webhook`, `mqtt`) and are registered in `shared/integrations/registry.py`; `describe_connector` feeds the frontend so it names no provider. `TransientFailure`, `PermanentFailure` and `Skipped` come from `shared/notifications/dispatch.py`.

`deliveries.py` is the mechanism (ADR 0014): `matches` applies the integration's filters to an `ObjectRef`; `enqueue` inserts queued rows with `ON CONFLICT DO NOTHING` on (integration, object type, object id, object version), records stale events as skipped and drops stale positions; `attempt` runs one try, sets the next attempt from `backoff_seconds` (30 s doubling to 6 h, `MAX_ATTEMPTS` 30) or the final status, keeps a compact trace per delivery and the integration's `last_delivery_at` and `last_error`; `backfill` walks a date range per object type in batches and commits progress on `integration.backfill`; `requeue` is the manual retry.

`services/integration/protect_integration/worker.py`: `handle_message` turns `position.created` (payload only), `event.created` and `measurement.created` (rows loaded) into refs and enqueues them for the project's enabled integrations (`IntegrationCache`, re-read every 30 s), then the bus message is acknowledged. `delivery_loop` calls `deliver_due` every two seconds: due rows in order, one integration's share of a cycle ends at its first transient failure, disabled integrations get their rows skipped. `handle_backfill` runs the backfill for `integration.backfill_requested`. The API (`routers/integrations.py`) only writes rows, publishes the backfill request and runs test sends inline.

## AI clients (MCP)

Architecture 27, ADR 0015. `services/mcp/protect_mcp` is a streamable HTTP MCP server built with the official SDK (`MCPServer`), stateless, at `/mcp` on port 8001 (`protect-mcp`). It never opens the database: every tool calls the API through `protect_mcp/api.py` with the client's own bearer and names the tool in `X-Protect-MCP-Tool`. `server.py` holds the tools (bounded reads: projects, entities, devices, positions, aggregated measurements, events, traces, plus the `search` and `fetch` pair ChatGPT expects), the `smartparks://` resource templates and the two prompts. `auth.py` verifies access tokens locally (`shared/oauth.py`). The protected resource metadata is served at both RFC 9728 paths; a request without a valid token gets 401 with `resource_metadata` in `WWW-Authenticate`, one without every read scope gets 403 with `insufficient_scope`.

The API is the OAuth 2.1 authorization server (`protect_api/oauth/`): `routes.py` mounts the SDK's authorize, token, registration and revocation handlers under `/api/v1/oauth` and serves `/.well-known/oauth-authorization-server`; `provider.py` implements the SDK's provider protocol on `oauth_clients`, `oauth_authorization_codes` and `oauth_refresh_tokens` (migration 0009): clients by client id metadata document (fetched from the client id URL, validated, cached an hour) or dynamic registration, consent requests that `/oauth/consent` in the frontend approves or denies (`POST /oauth/consent/{id}/approve|deny` return the redirect), JWT access tokens (`shared.oauth.mint_access_token`: audience the MCP URL, subject the user, `client_id` and `scope` claims, one hour), refresh tokens hashed and rotated. `scopes.py` is the AI action policy: `GET` only, one scope per path family, everything else refused. `middleware.py` admits an MCP token only through that policy, stores it in `mcp_access_var`, and writes an `mcp.request` audit row (user, client, tool, path, status) per request; the JWT strategy in `auth/users.py` accepts the MCP audience only when the middleware admitted the request. `GET /oauth/connections` and `POST /oauth/connections/revoke` back the Connected AI clients page.

Locally: `docker compose up -d` starts `protect-mcp`; the MCP inspector connects to `http://localhost:8001/mcp` (add `http://localhost:6274` to `CORS_ORIGINS`), Claude Code with `claude mcp add --transport http protect http://localhost:8001/mcp`. `tests/mcp` drives the MCP ASGI app with the API app in process over `httpx.ASGITransport`. User documentation: `docs/mcp/`.

## Gateways

`shared/models/network.py`: `Gateway` (server level, unique on data source and provider id) and `DataSourceCursor`. `shared/ingest.py` keeps the registry: `upsert_gateways` for every reception (online, last seen, a location from the reception's `location` attribute, the shape every adapter normalises per-gateway coordinates to; `location_source` and `location_at` say whether a platform list, a reception or an administrator placed the gateway, and an `admin` position is never overwritten, migration 0017) and `apply_gateway_update` for `GatewayUpdate` messages (`InboundMessage.gateway`, `external_id` None): stored as a source event without a device, `processing_status` processed, no bus message. The ChirpStack adapter parses `gateway/+/event/stats` and `gateway/+/state/conn` and its management connector's `list_gateway_updates` backs the gateway sync (`shared/connectivity/gateway_sync.py`, decision D176): `gateway_lister(source)` finds that method, `sync_source_gateways` applies one list, `sync_all_gateways` is the pass over every enabled source with its API channel on, committed per source, run by the ingest service's `gateway_sync_loop` two minutes after start and daily, and by `POST /data-sources/{id}/sync-gateways` for one source at once. A row `apply_gateway_update` created earlier in the same session reads its `stats` and `attributes` defaults as None until refreshed, so the merges start from an empty document. ChirpStack transports (D131): `grpc_api.py` has `_ChirpStackCalls` with every call the adapter makes on top of one `_unary(service, method, request, response_cls, what, not_found_ok)`; `ChirpStackGrpc` runs it over native gRPC (stubs on a channel, the test fakes patch `api.<Service>Stub`), `ChirpStackWeb` over grpc-web with httpx (a length-prefixed frame per message, the status from the `grpc-status` header or the 0x80 trailer frame, `_decode_web_frames`; `transport` is injectable for tests), `client_for(url, token)` picks by scheme and `is_api_url` accepts both. A tenant API key cannot list tenants (UNAUTHENTICATED), so `complete_config` reads the tenant id from `/tenants/<id>` in the address's fragment first and only asks the key otherwise. ChirpStack's HTTP integrations (phase 17, D125 to D127): `grpc_api.py` has `get_http_integration` (None on not found), `create_http_integration` and `update_http_integration`; `ChirpStackManagement.connect_applications(url)` merges the URL into every application's integration with `merge_endpoints` (kept, replaced after a token rotation, or appended; headers and other URLs untouched) and `integration_status(base)` reports `connected`, `other` or `none` per application (`endpoint_state`); the adapter sets `webhook_token_in_query` and `keeps_webhook_token`, so `create_data_source` and the token rotation keep `webhook_token` in the encrypted credentials, which `POST /data-sources/{id}/connect-applications` reads (409 without it); `describe_adapter` exposes `connects_applications` and `quick_setup` (D128: an adapter's `quick_setup` names the address and credential keys, the channels that start on and a hint; the form shows those two fields and folds the rest under Advanced; `complete_config(context)` on the adapter derives the rest on create and update through `_complete_config`, a 422 with the reason when it refuses; ChirpStack keeps the web address as `api_url` and reads `tenant_id` from the address's `/tenants/<id>` fragment, `list_tenants` only as a fallback for a global key). `connect_applications(url, dry_run=True)` reads and reports without writing (D129); the endpoint takes `?dry_run=true`, skips the audit then, and an optional body `{application_ids}` limits the write to those applications (`only` on the connector); `GET /data-sources/{id}/applications` returns `integration_status` per application (state, other URLs, header names) and `POST .../disconnect-applications` calls `disconnect_applications(base, only)` (our entries out, an integration left empty deleted through `delete_http_integration`); `components/admin/ApplicationsDialog.tsx` (D132) lists them with Connect and Disconnect per row, one request per application so each row spins on its own, and "Connect the other N" in sequence. `webhook_authenticated` in `transports/http.py` takes the header first and, for adapters with `webhook_token_in_query`, the `?token=` as well, so a stale header next to a right URL token passes (an integration set by hand keeps its old header after a rotation). `routers/gateways.py` aggregates `gateway_receptions` per project window: `project_gateways` lists every gateway of the data sources the project's devices have an identity on (decision D175; the all scope the whole registry), the hearing ones busiest first and the silent ones by last seen, the page filters by source client-side (`?source=`), gateway detail with the devices heard, and `connectivity` per device (gateways heard, best gateway and its share of uplinks, mean signal). Coverage (`routers/coverage.py`) joins positions to the receptions of the same source event and returns them per viewport as points, the newest `MAX_POINTS` (D123), or as `ST_HexagonGrid` cells sized by zoom on `mode=hexagons`. The query starts from the receptions: the devices heard in the window, then a lateral positions scan per device, which uses the compressed chunks' device index; a bounding-box scan over every positions chunk of the window decompressed them all (seconds per 90 days in the all scope, 0.5 s this way). The map cancels the request of a viewport that is gone (`signal`), settles 600 ms after a move, and the panel shows the error with a retry instead of a lasting "Loading…".

## Application platforms

Platforms whose data arrives decoded (Traccar positions, AddaxAI Connect detections) deliver the generic JSON shape (`time`, `lat`, `lon`, `speed` in m/s, `measurements`, `state`, `events` with optional `lat`, `lon`, `description`) with the original record under `raw`, and their devices use the Generic JSON driver, whose `decodable_event_types` include `position`, `event`, `state` and `detection`. That keeps adapters ignorant of devices and drivers ignorant of networks. The driver also declares `PLATFORM_COMMAND` (`type` plus `attributes` encoded as JSON), which a command connector such as Traccar's maps to the platform's command API.

Polling connectors keep their position in `data_source_cursors` through `DataSourceContext.cursors` (`DatabaseCursorStore` in production, `MemoryCursorStore` in tests). `POST /data-sources/{id}/cursor` writes `{"since": ...}`, which the connector honours at its next poll and then replaces with its own state. The AddaxAI Connect connector pages `GET /api/images` newest first from `captured_after`, rescans an overlap window daily, and logs in again on 401 (the JWT lives an hour).

## Backups and recovery

Architecture 28, decisions D72 to D75, `docs/operations/backup-and-recovery.md` and `restore-guide.md`. pgBackRest runs inside the database container (the TimescaleDB image ships it): `docker/postgres/pgbackrest.conf` holds the stanza and the container paths, everything per deployment arrives as `PGBACKREST_*` environment variables from compose (`BACKUP_*` in `.env`). `docker/postgres/pgbackrest-wrapper.sh` is mounted as `protect-pgbackrest` and is the only way pgBackRest is called: it drops an archive-push while `BACKUP_ENABLED` is false, removes options compose had to leave empty and S3 options for a non-S3 repository (pgBackRest refuses both), creates the log and spool directories in the `pgbackrest-state` volume, and is the `cmd` pgBackRest writes into a restored cluster's `restore_command`. PostgreSQL always runs with `archive_mode=on` and `archive_timeout=900`; the wrapper decides whether a segment goes anywhere.

`scripts/backup.sh database full|incr`, `objects` (the `object-mirror` compose service in the backup profile: `mc mirror` of every bucket to the backup bucket, never deleting on the remote, `MIRROR_DIRECTION=restore` for the way back) and `check` record every run through `python -m protect_api.backup record` (`backup_runs`, migration 0010; a pgBackRest info document on stdin is summarised) or `integrity` (the newest 500 referenced objects per store against the backup bucket with the MinIO client). `scripts/restore-verify.sh` restores the newest backup into a second compose project (`docker/backup/verify.yml`: own network, container names and volumes, no ports, archiving off, a local repository read from the production state volume, and the production stack's `migrate` and `api` images by name with `pull_policy: never`, so the test migrates with the code that is live; the migration output stays in the log), replays WAL, migrates, starts the API, checks health, row counts and object references, records a `restore_test` run and removes the project. `scripts/restore.sh` is the clean-server recovery. `shared/backup.py` assesses the state (runs plus `pg_stat_archiver`) for the Backup and recovery page (`routers/backups.py`) and for the `SYSTEM_BACKUP` findings of the rules service's system checks. Ansible installs the schedule as cron jobs when `backup_enabled` is set.

## Observability

Architecture 26.8, `docs/operations/observability.md`. `shared/telemetry.py` installs the OpenTelemetry SDK when `OTEL_EXPORTER_OTLP_ENDPOINT` is set (`configure_telemetry(service)` in `Worker.__init__`, the API and the MCP app): OTLP/HTTP exporters, SQLAlchemy, httpx and redis instrumentation, FastAPI in the API. `RedisStreamsBus._handle` wraps every message in a span with `protect.trace_id` and records `protect.bus.messages` and `protect.bus.handler.duration`; `Tracer.start` puts the processing trace id on the current span. The compose profile `observability` runs `grafana/otel-lgtm`. `protect_api/health_areas.py` computes the per-area System Health (architecture 26.2) that `GET /system/health` returns as `areas`; `protect_rules/retention.py` applies the per-class trace retention daily from the rules ticker.

## Deployment

`ansible/playbook.yml` with the roles security, docker, nginx, ssl, dev-tools, app-deploy and security-check, mirrored from AddaxAI Connect without FTPS and GPU: firewall with SSH, HTTP and HTTPS only; sshd hardening in a `01-` drop-in read back with `sshd -T`; security updates with a reboot at 04:30 only when required; fail2ban on sshd; nginx in front of the frontend container and the API with rate limits, websocket upgrade for `/api/v1/ws/` and a generous limit on `/api/v1/ingest/`; certbot without an interactive pause (the playbook refuses when the domain does not resolve to the host); checkout of the newest `vX.Y.Z` tag unless `git_version` is set; `.env` written from host vars; `docker compose up` with the migrate service; a daily cron for `scripts/security-status.sh`. `scripts/verify-server.sh` is the read-only gate after an update: containers versus `docker compose config --services`, Alembic head, health and version, worker heartbeats from Redis, error lines in the logs. Only the `*.example` files are committed; a private repository holds the filled-in inventory and the vault-encrypted host vars.

## Platform expansion (phase 13)

`shared/connectivity/adapters/tts/` is The Things Stack: `parse_message` maps the webhook documents (`uplink_message`, `join_accept`, the downlink events, `location_solved`) to inbound messages with gateway receptions, `TtsCommands` pushes to the application's downlink queue with a `smartparks-protect:<command id>` correlation id that the downlink events echo as `queue_ref` in the provider metadata (`apply_provider_signal` now also matches on that), `TtsManagement` lists devices and gateways with connection stats. `actility_thingpark/` subclasses the KPN adapter. `cra_iot/` (D90) is České Radiokomunikace: `parse_webhook` unwraps the `{type, data, tech, tags}` envelope and `parse_message` reads the LORIOT-shaped message (`gw` as the uplink by default, `rx` ignored, `geo` as a location), `CraClient` takes a token from the CRA single sign-on with the password grant and caches it, `CraCommands` posts to `/lora/devices/{EUI}/down/messages`, `CraManagement` pages through `/lora/devices`. `shared/integrations/connectors/earthranger.py` is the direct EarthRanger connector; `DeliveryItem.previous_external_id` (set by `load_item` from the last sent delivery of a lower object version) lets a connector update instead of duplicate. `connectors/wildlifenl.py` is the WildlifeNL connector (D88): readings and detections from the platform's open source API, species resolved by name against a cached `GET /species/`, one detection per species; `DeliveryItem.device_identity` (the device's most recently seen, not ignored external identity, loaded by `load_item`) is its default sensor id. `connectors/ferustracker.py` (D89) renders the document the Node-RED flow posts to ferustracker.nl (`devEUI`, `fPort`, `tags.payloadType`, `objectJSON` with the decoder's field names per payload family, `provider`, `site`) from positions and measurements; `DeliveryItem.device_type_key` selects the payload type. Movebank is two export datasets in `shared/exports/datasets.py` (`movebank_events`, `movebank_reference`). `shared/models/platform.py` holds `server_settings`, `mcp_pending_actions`, `project_icons` and `dashboards` (migration 0013). `routers/platform.py`: the manual event endpoint (`create_manual_event`, used by people and by the AI action endpoint), SVG icon validation (`validate_svg`) and dashboards; `routers/mcp_actions.py`: the AI action policy (`ACTIONS`, `DEFAULT_POLICY`, `load_policy`), `POST /mcp/actions` and `.../confirm` (ADR 0019). `protect_api/oauth/scopes.py` allows MCP tokens those paths and nothing else that writes; `shared/oauth.py` defines the write scopes. The MCP server's write tools call `ProtectApi.post` and check the token's scope before asking the API. Frontend: `stores/icons.ts` (loaded by the layout, read by `Icon`), `pages/project/DashboardsPage.tsx` with `components/dashboards/tiles.tsx`, `pages/admin/AiPolicyPage.tsx`.

## Production hardening (phase 14)

Data sources are their channels: `DataSource.channels` holds per-channel switches (migration 0015) that `shared/connectivity/channels.py` enforces in the ingest runner (`stream_channel_key`), the webhook (`webhook_channel_key`), command routing and the management actions (`api_channel_key`); the form is `components/admin/DataSourceForm.tsx`, fields generated from `config_schema` and grouped by the channels' `config_keys` and `credential_keys` (required) and `optional_keys` and `optional_credential_keys` (shown beneath them); a test checks every named key exists in the adapter's schemas. Adapters declare `channels` (or `channels_of` in `shared/connectivity/registry.py` derives them from the adapter's flags); `GET /data-sources/{id}/status` combines configuration presence, the last 24 hours of source events per ingestion method, the connector state the transports write to Redis through `shared/connectivity/state.py` (`report_connector`, read by the API) and the last Test connection outcome (`report_api_test`), and holds back capabilities of unconfigured channels. The ChirpStack adapter picks its transport by the scheme of `api_url` (`grpc_api.py`, `client_for`): `https://` speaks grpc-web through the web UI's own path, `grpcs://` and `grpc://` native gRPC (D131). `routers/admin_traffic.py` is the server-wide traffic page's API (inbound across sources through `traffic_rows` from `routers/network.py`, outbound deliveries and commands with project and integration names, the hourly summary); the frontend page is `pages/admin/AdminTrafficPage.tsx` with three tabs. `shared/exports/cleanup.py` (`expire_exports`) is the retention sweep the export service runs hourly through `Worker.background`; migration 0014 adds the `expired` status.

Security (D94): `protect_api/ratelimit.py` is an ASGI middleware over `shared/ratelimit.py` (Redis fixed windows; `RATE_LIMIT_*` settings; the test suite raises the limits in `tests/conftest.py`); `tests/api/test_access_matrix.py` walks every OpenAPI operation with an anonymous caller, a viewer of another project, a viewer of the own project and an AI client with read scopes, so a new endpoint without its dependency fails CI; a viewer write that is allowed by design goes into `VIEWER_WRITES_ALLOWED` there. Permissions belong in route dependencies (`require_permission`), never inside the handler after the body is parsed. CI runs `pip-audit` over the exported lock and `npm audit` (job `audit`, also weekly). Docs (28.8): `scripts/docs_check.py` checks internal links and anchors in the built site, Mermaid fences and the MCP tool reference; the docs CI job runs it after the strict build. Organizations (D92): `admin.py` CRUD under `/admin/organizations`, `Project.organization_id` (server admins only in `update_project`), `?organization_id=` on the project list. Gateway name by hand (D247): `routers/gateways.rename_gateway` (`PATCH /projects/{id}/gateways/{gateway_id}`, scope permission `project:write`, `_in_scope_or_404` shared with the location endpoint, an audit row `gateway.renamed`, an empty name clears `name_override` so `display_name` falls back to the platform's); `components/network/GatewayNameDialog.tsx` is the dialog the Gateways page and the map's `GatewayPanel` open. `lib/gateways.ts` holds `isRelay`, `locationSourceLabel` and `gatewayCoords`, which both places share. Gateway location by hand (D239): `routers/gateways.set_gateway_location` (scope permission `project:write`, the gateway's source in `visible_source_ids`, `_set_location` shared with the admin override, an audit row `gateway.location_set`), `schemas/integrations.GatewayLocationRequest`; `components/network/GatewayLocationDialog.tsx` over `MiniMap` with `onPick` (interactive, crosshair, a click reports [lon, lat]) and `fallback`; the Gateways page and `GatewayPanel` open it. Gateway Mesh relays (D237): `grpc_api.list_relay_gateways` (`_pages` with `method="ListRelayGateways"`), `gateway_updates_from_relays` (`kind: relay`, name None when it equals the id), `ChirpStackManagement.list_gateway_updates` joins both and logs an `ApplicationError` from the relay call away; `GatewaysPage.isRelay` reads the mark. Data source scopes (D235, D236): `routers/gateways.visible_source_ids` (identity sources of the scope's devices plus `DataSourceProjectScope` rows of the project) filters the project's gateways, `routers/search.py` takes the same union, `routers/attention._suggested_projects` fills `UnknownIdentity.suggested_project_id` and `_name` when a source is assigned to exactly one project; the attention page pre-selects it (`commonSuggestion` for the bulk dialog). `components/data/DataTable` takes `cardOf` for a card per row on a phone. Environmental layers (D245, D246): `shared/analysis/environment.py` is the boundary (`LayerSample`, `PROVIDERS`, `provider_for`, `geometry_hash`, `week_start`, `weeks_between`, `nearest_week`, `sample_weekly`, which reads the `environment_samples` cache, asks the provider only for the span of whole weeks no geometry has yet, merges the answer into what the cache held, and stores nothing for a week that has not ended, since a cloud-free pass may still come; a provider that answers a different number of series than geometries asked raises rather than pair them up wrongly, reviewed 2026-09-18); `shared/analysis/providers/` registers what the settings allow (`copernicus.py`: the client credentials flow with the `openid` scope, the bearer as `oidc/CDSE/<token>`, one batch job with `mask_scl_dilation` and a weekly mean, the asset fetched without our authorization header because it is a signed URL). `modules/grazing.landscape_figures` writes `ndvi_mean`, `ndvi_change`, `ndvi_valid_share` and `ndvi_level` per area, the `vegetation` table and the `ndvi_weekly` chart, and turns any provider failure into a warning. Migration 0039. The account is a server setting an admin fills in (decision D250): `environment.PROVIDER_SETTING` (`server_settings` key `environment_providers`), the secret a Fernet token through `shared/secrets.py`, `stored_providers(session)` builds what is enabled and `configured_provider(session, layer)` prefers it over the statically registered ones, so a change takes effect without a restart; `routers/environment.py` is the server admin API (list, `PUT .../copernicus` where an omitted secret keeps the stored one, `POST .../test` which asks for a token and the collection and costs no processing quota through `CopernicusProvider.check`) behind `pages/admin/EnvironmentPage.tsx`. The environment settings `COPERNICUS_CLIENT_ID` and `COPERNICUS_CLIENT_SECRET` with `settings.landscape_configured` remain as the fallback. `GrazingParameters.landscape` leaves the layer out of a run; the form's switch writes it. The vegetation layer is a mosaic on the analysis grid (Tim, 2026-09-18: one flat colour per area is no map): `grazing.vegetation_grid` lays a `LocalGrid` over the areas and keeps the cells whose centre is inside one, doubling the cell size until the selection fits `MAX_VEGETATION_CELLS` (600) so a large park draws coarser rather than refusing; `landscape_figures` sends the area polygons and the cells to the provider in one `sample_weekly` call, so the areas keep their exact mean and the mosaic costs no second job (`copernicus.MAX_GEOMETRIES` is 700 for it), and `_write_mosaic` writes `summary["vegetation"]` in the shape of `summary["intensity"]`. The frontend's `vegetationFeatures` mirrors `intensityFeatures` and colours by rank, not by value: each of `VEGETATION_RAMP`'s five steps holds a fifth of the cells (`level` is the cell's place among the others and `vegetationBreaks` gives the index each colour starts at, for the legend), because a landscape is not spread evenly over its range and a plain low-to-high scale left nine cells in ten in one green; `ensureVegetationLayers` draws it in its own source under the use intensity, off by default, and its cells answer a click. The palettes carry the meaning (Tim, 2026-09-18): `VEGETATION_RAMP` is bare to green, which is how a satellite index is read everywhere, and `INTENSITY_RAMP` is warm, so green is what the land grows and warm is how hard it was used. Two green ramps over one another could not be told apart; warm was measured as the best of the candidates against the vegetation ramp under both red-green colour blindnesses, and a blue ramp was rejected because its light steps come nearer the base maps’ water than water comes to land. `PRESSURE_RAMP` is warm for the same reason, and `report/mapimage.py` holds the same five colours so a report reads like the map. A ramp's thresholds sit beside it (`INTENSITY_STEPS`, `VEGETATION_STEPS`) and `rampExpression` builds the MapLibre `step` from the pair, so a layer and its legend cannot drift; `MapLegend` in `ResultMap.tsx` draws every legend from a title, a ramp and the value each colour starts at, which is what keeps two of them aligned. Everything along the bottom of `ResultMap` (the panel of a clicked object and the legends) sits in one bottom-anchored column, so no two overlap at any width and the bottom right stays free for the zoom controls. `bindFeatureClicks` in `components/map/layers.ts` yields to `ABOVE_FEATURES` (the marker and cluster layers) through `queryRenderedFeatures` at the click point: a map calls the handler of every layer under the cursor whatever the drawing order, and the feature's is bound last on the live map, so without it an entity inside a geofence opened the geofence. A new clickable layer that can sit over an area belongs in that list. `lib/portalContainer.ts` is where a floating menu renders: `document.fullscreenElement` when there is one, else the default; `ui/dropdown-menu.tsx`, `ui/select.tsx` and `ui/popover.tsx` pass it as the Radix portal's `container`, because a browser paints only the full-screen subtree and a menu in the page body opens invisibly over a full-screen map. A regenerated shadcn component loses that line. Tests: `tests/shared/test_analysis_environment.py` (including the recorded live answer in `tests/fixtures/openeo/`) and the landscape case in `tests/api/test_analysis_grazing_run.py`. AWT tracker (D241): `shared/device_drivers/awt` (`parse_frame`, `decode_latitude`, `decode_longitude`, `AwtDriver`), `scripts/awt_golden.py` runs `tests/fixtures/payloads/awt/decoder.js` with node over the frames it builds and writes `golden.json`, `tests/shared/test_awt_driver.py`; `explanations.ERROR_FLAGS` carries the AWT flags. D242: `declared_intervals` reads any state's `settings` dict (`fix_interval`, `status_interval` in `expected_intervals`) beside the TLV frames. Server texts in the reader's language (D240): `shared/i18n` (`resolve_language`, which reads the `Accept-Language` quality values rather than the order the parts stand in and skips a `q=0`, reviewed 2026-09-18; `translate`: exact catalogue entries first, then the catalogue's `{placeholder}` templates as patterns, the text itself otherwise; `nl.py` is the Dutch), `protect_api.deps.language` reads `Accept-Language`, `routers/events.event_read`, `alert_read` and the map feed translate title, description and explanation (`explain_event(..., language)`), `routers/analyses._read` the warnings of a run; the frontend's `api/client.ts` sends the header and `main.tsx` refetches on `languageChanged`. A new fixed server text needs its entry in `nl.py`; `tests/shared/test_i18n.py` checks the explanations, the flags, the reset reasons, the folded warnings and the rule templates. Timezones: `lib/timezones.ts` (`allTimezones` from `Intl.supportedValuesOf`, `isTimezone`, `zoneLabel` with the offset, `orderedTimezones`) feeds `components/TimezoneSelect.tsx`, the one picker for the project forms, the explorer strip and the export dialog; `readRecordsState` and `readExploreState` take a `defaults.timezone` (the project's) for an address that names none; the API's `schemas/access.valid_timezone` refuses a name `ZoneInfo` cannot load. Translation (D93): `src/i18n.ts` initialises i18next with the English catalogue `src/locales/en/translation.json`; every string a person reads goes through `t()` from `useTranslation()` in components and hooks or `i18n.t()` at module level, with the English text as the key; ESLint's `i18next/no-literal-string` refuses JSX text and the listed attributes; `npm run i18n:extract` regenerates the catalogue and `npm run i18n:check` fails when it is stale (CI); English and Dutch ship (`src/locales/nl/translation.json`, 2,124 strings, Tim 2026-09-17); a language is added by dropping its catalogue in `src/locales/<code>/`, importing it in `src/i18n.ts` and listing it in `LANGUAGES`; `i18n:check` warns (never fails) about the strings another catalogue lacks or no longer needs, since English stands in for a missing string. Terms kept consistent in Dutch: apparaat (device), entiteit, gateway, fix, uplink, databron, bronbericht (source event), gebeurtenis (event), alarm (alert), toewijzing (assignment), kaartobject (feature), spoor (track), curatie, aflevering (delivery), analyse (run), Aandacht nodig (Needs attention), Dataverkenner, livekaart. Benchmark (D91): `generate.py` writes in time blocks (`--block-days`) with a barrier between workers, and `--compress` compresses chunks behind the write frontier; inside a container run it as `/app/.venv/bin/python`. Release process: `docs/operations/release-process.md`. Docs site: the `pages` job of `.github/workflows/ci.yml` publishes `site/` (the strict build the `docs` job made) to GitHub Pages on every push to `main`; the repository's Pages source is "GitHub Actions".

## Data curation

`shared/curation/effective.py` defines the effective value (ADR 0018, decision D80): `effective_time`, `effective_geom`, `effective_number` and `effective_value_num` (coalesce of the `curated_*` overlay column and the original), `in_window` and `before` (time predicates written as a disjunction so uncurated rows use the ordinary time indexes with chunk exclusion and curated rows the partial index on `curated_time`), and `visible` (`valid`). Every reader of positions and measurements goes through these: the map track, the positions list, `shared/analytics.py`, the analytics rows and metrics, `shared/rules/data.py` and `replay.py`, `shared/exports/datasets.py` (with `view=original` reading the original columns) and `shared/integrations/deliveries.py` (`load_item`, `curated_ref`). New readers must too.

`shared/curation/apply.py`: `CURATABLE` (position: time, coordinates, valid; measurement: time, value, valid), `normalize_value`, `apply_correction` (writes the overlay, supersedes an active correction on the field, reruns the attribution on a time change, bumps `curation_version`, flags sent deliveries stale), `revert_correction` (restores the value before, refuses while a newer correction is active, brings a superseded one back) and `recompute_current_state`. `shared/curation/jobs.py`: `Transformation` (time offset, validity, value offset or scale), `preview` (count, samples, impact on attribution, deliveries and rules over the first 5,000 rows), `apply_job` and `revert_job` (batches of 500 rows in their own transactions, progress on the job row, a replay report on request) and `run_job`, the handler of `curation.job_requested` in the export service. The API is `routers/curation.py` (project setting `curation_requires_approval`, decision D81; the proposer never approves their own); stale deliveries are listed with `?stale=true` and resent with `POST /integrations/deliveries/{id}/resend`, which enqueues the object's current `curation_version` as a new idempotency key. Migration 0012. Docs: `docs/analytics/curation.md`.

## The all-projects scope

Decision D115, ADR 0022. `protect_api/deps.py`: `ScopeContext` and `get_scope_context` accept the reserved project id `all` for a server admin (403 otherwise) or a project id; `require_scope_permission(permission)` is the dependency the supported reads take instead of `require_permission`, and they filter with `context.where(Model.project_id)` (one project, or "any project" in the all scope; never the system scope of events without a project). Raw SQL in the tiles and the coverage takes `context.project_id` and tests `CAST(:project_id AS uuid) IS NULL`. Every other `/projects/{project_id}` endpoint keeps a UUID parameter, so `all` is a 422 there; `tests/api/test_access_matrix.py` lists the supported reads in `ALL_SCOPE_READS` and checks the rest refuses the id. `context.where(column, unassigned=True)` lets the all scope take data of no project as well (the device layer and device tracks, decision D120). `realtime.py` keeps the sockets of the all scope under `ALL_PROJECTS_KEY` and sends them every project's messages. Frontend: `lib/scope.ts` (`ALL_PROJECTS`, `isAllProjects`, `projectFor`), `navigation.ts` marks the items that work in the scope (`allScope`, `sectionsFor`), `AppLayout` sends any other project page opened with `all` to the map, the switcher lists All projects for a server admin, the lists gain a Project column, and the layers panel takes `projects` to nest project, group, entity (`projectLayerOf`, `ungroupedLayerOf`, `withProjectShown` in `layerChoices.ts`).

## Profile pictures

Decisions D110 and D194. `shared/pictures.py` turns any JPEG, PNG or WebP into a centre-cropped WebP with Pillow (rotation metadata applied): a 512 px square for a device, `PICTURE_LANDSCAPE` (1200 by 900) for an entity, whose picture is a card on its page (`components/entities/PictureCard.tsx`) and never its icon; `protect_api/pictures.py` reads the upload within `PICTURE_MAX_BYTES`, stores the square in `MINIO_BUCKET_PICTURES` under `entities/<id>.webp` or `devices/<id>.webp`, and serves it with `Cache-Control: private, max-age=86400`. `Entity` and `Device` carry `picture_key` and `picture_updated_at` (migration 0021); reads and the map's current-state features expose `picture_updated_at`, which the frontend uses as the version: `hooks/usePicture.ts` fetches the picture once per version with the bearer token (an `img` cannot send it) and keeps an object URL, `components/common/ObjectPicture.tsx` shows it round with the type icon as the fallback, `PictureEditor.tsx` is the header control that sets and removes it. Permissions follow the object: `entities:write` for entities, server admin for devices; every change is audited.

## Records

Phase 20, decisions D142 to D146. `shared/records.py` is the one implementation the API's records read and the records export share: `RecordSelection` (the caller's project filter as a function of a model's `project_id` column, so the filter of one table never joins the other hypertable in; entity and device ids; the window; `include_invalid`), `keys_statement` (the union of the positions' and the measurements' effective times per device, the moments), `count_keys`, `metric_keys` and `state_keys` (a wide export's header), and `fill(session, selection, keys)`, which reads the positions, measurements and state history of a page's devices over the page's time span once each through the owner and time indexes and matches them to the moments. `routers/records.py` pages the moments newest first on a `(time, device)` cursor (base64 of the pair, 1,000 per page) with `GET /projects/{id}/records` and answers the count for the progress bar with `/records/count`, in the all scope. The export dataset `records` (`_records` in `shared/exports/datasets.py`) streams the moments oldest first and fills them a page at a time, wide (`m_<key>` and `s_<key>` columns learned by `load_lookups` from the selection) or long (`records_layout`); `count_statement` counts the moments for the direct bound. Frontend: `lib/records.ts` (the URL state with `mode=table`, `windowFor`, `columnsOf` over the loaded rows, `cellOf`, `seriesOf`), `hooks/useRecords.ts` (pages loaded in sequence into state keyed on the selection, so a change shows an empty list without a state reset in an effect; Stop aborts, Load again bumps a generation), `components/records/` (the virtualized `VirtualTable` on `@tanstack/react-virtual`, `RecordDialog` for one moment); the chart and the map are the other two modes of the explore canvas (`?mode=chart`, `?mode=map`), `readExploreState` still opens the v2.4.0 `records` and `analysis` links as the table and the chart, and `writeExploreState` carries the mode so saved views land on their tab. The export dialog starts the job itself when the direct path answers 413 and polls it inline (`queryKeys.exportJob`).

Bounds answer rather than refuse (decision D148): `choose_resolution` returns the finest bucket that fits when the asked bucket is too fine, with `Resolution.requested` and its `note`; `_owners` in `routers/analytics.py` picks the given entities or devices, else every entity of the project or every device assigned to it today, and when more would exceed `MAX_SERIES` the first that fit by name, without touching the hypertable; the series response carries `notes`, `owners_shown` and `owners_total`, and the explorer shows the notes. Lists load page after page (decision D149): `hooks/usePages.ts` wraps an infinite query on the `next_cursor` of a `Page` and `components/data/LoadMore.tsx` is the footer; the rows behind a bucket, the entity, device and group lists and the admin's device list use them. Links into the records view go through `recordsHref(projectId, {entities, devices, at})` in `lib/records.ts`: with a moment, the twelve hours around it and `?at=`, which the records state keeps and `VirtualTable` turns into the marked row scrolled into view once it loads (`aria-current`). Explore as one canvas (phase 21, decisions D150 to D155): `lib/explore.ts` holds the state on top of the records state (`mode`, the chart's metrics, kind, x metric, bucket and aggregates; `readExploreState` accepts the `records` and `analysis` modes of v2.4.0 as the table and the chart), the chart data from the loaded rows (`chartGroups`, one group per metric and one series per owner, `scatterGroup`, `groupsFromSeries` for the aggregate read above `CANVAS_BOUND`), the tracks of the rows (`tracksOf`, `boundsOfTracks`, `pointsAt`) and `nearestRowTime` for the mark. `pages/project/ExplorerPage.tsx` is the strip (`components/explore/SelectionStrip.tsx`, foldable to a summary line; a phone starts folded once a selection exists), the mode switch and the canvas: `ExploreChart` (ECharts, one graph with a thin line per metric and owner, the axes by unit from `unitAxes` and `axisIndexOf` in `lib/explore.ts`, the palette and the line list in `lib/chartStyle.ts`, legend chips in React that filter the lines, one value card as the tooltip, the hover and the click converted from pixels, a marked moment from elsewhere shown as the axis pointer, the pinned one as a mark line), `ExploreMap` (the live map's layer helpers over the period, a resize observer on the container, the time slider, the ringed points at the marked moment) and `Drawer` (the table under the chart or the map, its height and fold kept per user, a bottom sheet on a phone). The marked moment is one state on the page: a hover carries its source so the drawer follows the chart and the map but not itself; a click writes `at` with a replace. The API's database connections carry PostgreSQL's `statement_timeout` (decision D147): `statement_timeout_seconds` on the settings, 0 by default, set to `API_STATEMENT_TIMEOUT_SECONDS` (120) for the API service alone in `docker-compose.yml`, so the workers keep their long statements.

## Analytics and exports

`shared/analytics.py` holds the aggregation statement the API and the export worker share: the bucket ladder and the automatic resolution (decision D41), the bound of 5,000 points per series and 20 series per request, and `aggregate_statement` built on `time_bucket`, `first` and `last`. The router is `protect_api/routers/analytics.py` (series, drill-down rows, metrics with data, saved views).

`shared/exports/` is the export engine: `ExportParameters` (what a job stores), `datasets.py` (one streamed query per dataset, rows as dicts, `yield_per` server-side cursor), `writers.py` (CSV, XLSX in write-only mode with sheet splitting, JSON, GeoJSON, GPX; rows in, bytes out) and `runner.py` (a job to a temporary file to MinIO, or a direct stream bounded at 100,000 rows). Progress writes go through a separate session, because a commit on the streaming session closes the cursor. The export service (`services/export`) consumes `export.requested`. Docs: `docs/analytics/`.

## Analysis

`shared/shared/analysis/` is the analysis subsystem (`docs/ANALYTICS_PHASE1_PLAN.md`): computed results over the core's tracking data, kept apart from it. No core module imports the package (`tests/shared/test_analysis_boundary.py` holds that), it writes only the `analysis_runs` and `analysis_geometries` tables, and `ANALYSIS_MODULES` switches it off. `base.py` is the contract (a module has a key, a version, a parameters model and `run(ctx, params) -> RunResult`; the result is a `ResultDocument` of summary, tables, charts and warnings plus the geometries the runner stores); `runner.py` runs one queued row with the statement timeout, the wall clock, progress and cancellation through a short session, and stores every outcome on the row (the step a module reports with its percentage is written to `analysis_runs.progress_step`, migration 0040, decision D249, and `RunStatus.tsx` draws the bar with it; it is cleared when the run finishes); `modules/` registers the modules on import (the worker and the API import it); `primitives/` holds what the modules share (`trajectory.py` streams a subject's device fixes into numpy arrays and gives steps, gaps, time weights and outliers; `spatial.py` the local metric grid, residence and visits, DBSCAN clusters and areas through PostGIS; `homerange.py` MCP and a binned Gaussian KDE with cell-union isopleths; `timeagg.py` the sun's elevation, local days and seasons); `quality.py` the warnings. `shared/geodesy.py` is the one haversine, shared with the rule evaluator and the satellite module. The analysis service (`services/analysis`) consumes `analysis.requested`, one run at a time, and expires unkept runs hourly. Frontend: `lib/analyses.ts` (the document types, the form in the URL, the module parameters), `components/analysis/` (the form, the run list and view, the result map over `components/map/analysisLayers.ts`, the cards, charts and tables, and `presentations.tsx`: a module's labels and its own blocks, shared by the page and the print view) and the pages under `analyze/`. The PDF report (decision D211, ADR 0033) is `shared/shared/analysis/report/`: `job.py` (`queue_report` marks the run row, `publish_report` puts `analysis_report.requested` on the bus after the commit, `run_report_job` is the export service's handler: it gathers the document and the geometries (the tracks stay off the paper), renders and stores the PDF as `analysis-reports/<run>.pdf` in the exports bucket, and writes `report_status`, `report_key`, `report_at` or `report_error` on the run, migration 0035; `remove_report` goes with a deletion and the expiry sweep), `render.py` (the Jinja template `templates/report.html` with `report.css` rendered by WeasyPrint; the English labels of both modules, the key figures table, the settings rows, `table_block` turning a table wider than `WIDE_COLUMNS` with few rows on its side), `charts.py` (matplotlib: line, bar, stacked and rose as SVG in the brand `PALETTE`) and `mapimage.py` (Web Mercator maths, `extent_for`; the base map is MapTiler's static map when `MAPTILER_KEY` is set and the key allows static maps, fetched with the server's public URL as referrer, else the standard OpenStreetMap tiles under the extent, `tile_grid` at most `MAX_TILES` and `stitch_tiles`, fetched four at a time with a proper user agent, unless `REPORT_TILES` is off; `draw_map` over the base or over a plain ground, with a scale bar, a north arrow and the base map's attribution). The API: `POST /projects/{id}/analyses/{run}/report` (`exports:create`, 409 while one is being made) and `GET .../report` (the PDF once ready). `RunView.tsx` offers "Make PDF report" under Export, polls while it is being made and shows "Download PDF" when ready. The image installs Pango, HarfBuzz and DejaVu for WeasyPrint (`docker/python.Dockerfile`), CI the same for the tests. `modules/grazing.py` reuses the trajectory, the time weights, the residence grid and the seasons; containment runs in shapely over the loaded arrays, the areas and their hectares come from PostGIS, and a module may offer `check(session, project_id, params)` that the estimate calls to refuse what the run could not use. Docs: `docs/analytics/movement.md`, `docs/analytics/grazing.md`.

Device performance (phase 28, decisions D213 to D220, ADR 0034, `docs/ANALYTICS_DEVICE_PERFORMANCE_PLAN.md`): the framework's subjects may be devices. `parameters.SubjectSelection` takes `device_ids` (at most `MAX_DEVICES`, 100), `device_type_id` and `all_devices`; a module declares `subject_kind = "device"`, which `routers/analyses.py` reads through `_subject_kind` to resolve the subjects with `_resolve_devices` (the devices assigned to the project at some point in the period, the member's `device_visible` scope, a refused id outside it), to count the fixes by `Position.device_id`, to store `device_ids` on the run and to hide a run outside the reader's scope. `base.Subject` carries `kind` and `tracked`; `primitives/trajectory.load_trajectory(..., by_device=True)` reads a device's own fixes. The primitives of the module: `primitives/levels.py` (`Threshold`, the named `DEFAULTS` per indicator key, `thresholds_for(health_fields)` laying the driver's `HealthField` bounds over them through `DRIVER_FIELDS`, `worst`, `ranks` with `WORSE_WHEN_HIGH`), `primitives/health.py` (`bucketed` daily or weekly figures, `slope_per_day`, `days_to`, `flag_shares` over status states, `is_status` against a settings frame, `reboots_from` the `device_reset` events, `versions_seen`), `primitives/intervals.py` (`decode_tlv_settings` by the driver's `catalog.json`, `merged_settings`, `expected_intervals`, `interval_report` with the missed share and the silences against three expected intervals) and `primitives/network.py` (`counter_gaps` and `lost_share` over a frame or MOMSN counter, `fold_receptions` into the best RSSI and SNR per uplink and the gateway shares). `modules/device_performance.py` reads per device and period, one streamed query each, the measurements of `METRIC_KEYS`, the state history, the reset events, the fixes, the invalid counts, the data sources with their channel from the source events, and per LoRaWAN source the receptions, the frame counters, the buckets and the joins, per Iridium source the satellite sessions; `health_figures`, `reporting_figures`, `gnss_figures` and `network_figures` fill a `DeviceFigures`, `build_document` writes the summary (`main`, `comparison`, `levels`, `ranks`, `devices`, `defaults`), the tables (`fleet` sorted worst first with `FLEET_COLUMNS`, `health`, `reporting`, `gnss`, `network`, `errors`, `reboots`) and the charts; the geometries are `coverage` (the convex hull of the fixes) and `gateway` (points with the share as `level`). Migration 0036 widens the run row's module check; `AnalysisModuleKey` has the key; `ANALYSIS_MODULES` defaults to the three modules. Frontend: `lib/analyses.ts` (`devices`, `deviceType`, `allDevices` in the form state, `devicePerformanceParameters`, `levelOf`), `components/analysis/DevicePerformanceForm.tsx`, `FleetTable.tsx` (`FleetTable`, `DeviceSections`, `LevelDefaults`, `LevelDot`) over the helpers of `fleet.ts` (`orderedSubjects`, `deviceLevel`, `showFigure`, `AREA_CARDS`), `presentations.devicePerformancePresentation` with the `charts` and `tables` overrides `RunView` now accepts, `pages/project/DevicePerformancePage.tsx`; `components/map/analysisLayers.ts` gained the `coverage` and `gateway` kinds, an `analysis-points` circle layer sized by the share, and the `analysis-fixes` layer coloured by `accuracyColor`, fed by `ResultMap` from the track response's `accuracies` (`TrackResponse.accuracies`, per vertex). The report (`report/render.py`): `DEVICE_PERFORMANCE_LABELS`, `_device_figures` (the fleet table as key figures, cells as `{text, level}` the template draws with `dot lvl-*`), `device_sections`, `DEVICE_PERFORMANCE_LIMITATIONS`, the `defaults` block; `mapimage._draw_shape` draws a Point as a marker sized by `Shape.level`. Tests: `tests/shared/test_analysis_device_performance.py` (the primitives on synthetic series), `tests/api/test_analysis_device_performance_run.py` (a healthy and a failing device through the API and the runner, the subjects by id, type and all, the scope), the device cases in `tests/shared/test_analysis_report.py`, `src/components/analysis/fleet.test.ts`. Docs: `docs/analytics/device-performance.md`. The forms (Tim, 2026-09-16): `FormState.groups` (URL `group`, repeated) is a standing choice on all three forms, `withGroupMembers` in `lib/analyses.ts` joins the members of the chosen groups (with subgroups) to the picks; the device performance form resolves its sources on the client with `deviceSelection` (devices by name, `entities` tracked, `groups`, `entityTypes` with subtypes, `allDevices`) and narrows them with `deviceType` as a filter, then `devicePerformanceParameters(state, now, selection)` sends `device_ids`, or `device_type_id` and `all_devices` for the whole fleet, which stays the server's to resolve; `useResolveSubjects` in `components/analysis/subjects.ts` resolves a type deep link only. The report's map has a legend (`map_legend` in `report/render.py`, the `legend` block of the template): the subjects in their colours and one line per kind of shape drawn. The expected interval (D225 to D227): `shared/domain/reporting.py` (`declared_intervals` from the sources in order of trust, `expected_fix_interval` for the device page anchored at the device's last fix) and `shared/domain/reporting_rules.py` (`learn_interval` with `LEARN_BIN_S` of a minute and `snap_interval` to `SENSIBLE_INTERVALS_S` within `SNAP_TOLERANCE`, `resolve_expected`, the TLV codec) in the core; `primitives/intervals.py` re-exports them for the module. Device settings known per device (D228 to D231, ADR 0035): `shared/models/settings.py` (`DeviceSetting`, migration 0038), `shared/domain/device_settings.py` (`record_setting`, the one upsert: a newer observation replaces, a frame confirms a `sent` command; `record_settings_frame` decodes a `port_3_tlv` state by the catalogue), the decoder's `_write_states` calls it (`settings_changed` on the outcome and the trace step; `ble` when the delivery came over WebBLE), `request_command` records a `sent` value for any encoded command whose metadata names a `setting`, the OpenCollar driver's `SET_SETTING` (any catalogue setting, `encode_setting_value` by type and range), `REQUEST_SETTING` (`cmd_send_single_setting` with the setting id, the way over LoRaWAN or satellite) and `REQUEST_SETTINGS` (`cmd_send_all_settings`, whole over Bluetooth, first part only over the air); the tab reads the whole table over Bluetooth through `useWebBle` (`session.requestSettings()` then `sync`, the same path as the Bluetooth card) when the device is connected in the browser; `GET /devices/{id}/settings` merges the catalogue (`driver_catalog`, `driver_catalog_document` with `firmware` and `versions`) with the rows, `PUT /devices/{id}/settings/{key}` records a person's value; `components/devices/DeviceSettingsTab.tsx` is the tab with the set and record dialog. The catalogue's `group`, `unit`, `since_firmware` and `description` were generated once from the BLE settings app's per-release files and the research document, with hand-written descriptions for the settings people change most; regenerate them the same way when a firmware release adds settings. Revised after the Pangolins run (D232 to D234): `primitives/health.battery_trend` returns a `Trend` (the slope only when proven: `MIN_TREND_DAYS`, `MIN_TREND_SLOPE`, twice the standard error; `kind` falling, rising, steady or unknown) and `days_to` answers None beyond `MAX_DAYS_TO`; the summary carries `battery_trend`; `modules/device_performance.split_missed_fixes` runs after the network figures and writes `missed_fix_network_share` and `missed_fix_device_share` from `expected_fixes`, `fixes`, `lost_uplinks_share` and the failed `attempts`, the level on the device's share only (`DEFAULTS["missed_fix_device_share"]`); `FLEET_COLUMNS` carries the device's share; `DETAILS_COLUMNS` is the `details` table; fix success is the successes (the fixes or the `gnss_fix` trues, whichever is more) over those plus the `gnss_fix` falses. Frontend: `fleet.trendFallback` gives the word for an empty slope or days cell, `FleetTable` takes `tableKey` ("fleet" or "details"), the presentation's `tables` render draws the details table and the `TABLES_SHOWN` (errors, reboots). Report: `trend_word`, `DEVICE_TABLES_IN_CARDS` keeps the four area tables off the paper.

## Benchmark

`scripts/benchmark/generate.py --scale 0.01` loads a synthetic dataset at a fraction of the reference envelope (architecture 13.9) with COPY: eight benchmark projects, devices walking around a home range, four measurements per position. `scripts/benchmark/run.py --email ... --password ... --manifest ...` times the map, tiles, tracks, Data Explorer, exports and an ingest burst against the running stack and writes `docs/operations/benchmarks.md`. `generate.py --reset` removes the dataset. See `docs/architecture/scalability.md` for the reading of the results.

## Database migrations

Migrations live in `services/api/alembic/versions/`, named `<rev>_<slug>.py` with a four digit revision. They run synchronously through psycopg (`alembic/env.py` rewrites the async URL). `scripts/dev.sh migrate` applies them locally; in docker compose the one-shot `protect-migrate` container runs `alembic upgrade head` before the API starts (decision D30). `scripts/dev.sh revision "message"` autogenerates a draft from the models; hypertable creation, compression and retention policies are written by hand as explicit steps (see migration 0001). The test suite migrates the test database up at the start and down at the end, so every run is an up-and-down test. `alembic check` must report no drift; the Alembic environment ignores the time index TimescaleDB adds to every hypertable.

Hypertables cannot be referenced by foreign keys, so canonical rows carry `source_event_id` and `source_event_ingested_at` as plain columns.

## Authentication and access control

FastAPI-Users with JWT bearer tokens under `/api/v1/auth`. Registration is by invitation only (`POST /api/v1/auth/register` with the invitation token); the token proves the email, so accounts are verified on creation and the invitation's role becomes a membership. The JWT strategy adds `iat` and rejects tokens issued before `users.password_changed_at`, so a password change logs out other sessions. Password reset uses the FastAPI-Users flow; mail goes through `protect_api/mailer.py`, which logs instead of sending when SMTP is not configured or the recipient is not in `DEV_NOTIFY_EMAILS` on a non-production server without `MAIL_DELIVER_TO_ALL`; the invitation response then carries the reason and the registration link.

Dependencies in `protect_api/deps.py`: `require_server_admin`, `get_project_context` (reads `{project_id}` from the path, 404 for unknown projects, 403 without membership), `require_project_role(role)` and `require_permission(key)`. Permission keys and their mapping from roles live in `shared/permissions.py`. Every mutating endpoint calls `record_audit` in the same transaction.

## API conventions

- Everything under `/api/v1` (D29). `/api/health` and `/api/version` stay unversioned.
- Every list endpoint takes `limit` (max 500) and `cursor` through `protect_api/pagination.py` and returns `{items, next_cursor}`. A test fails when a list endpoint lacks `limit`.
- Geometry goes in and out as GeoJSON (`geometry` field); the helpers in `protect_api/crud.py` convert with shapely.
- Constraint violations become 409 with the constraint name in the message (`flush_or_409`).
- Credentials of data sources are encrypted with Fernet (`shared/secrets.py`, key from `CREDENTIALS_KEY`) and never returned.
- A device's `serial_number` is the manufacturer's serial; `protect_api/serial.py` fills an empty one from the first identity when the driver says both are the same (OpenCollar: the DevEUI), on identity creation, linking and onboarding from Needs attention (D101).

## Testing

```bash
uv run pytest -q                            # all python tests, needs the compose stack for `integration` tests
uv run pytest -q -m "not integration"       # unit tests only
uv run pytest tests/shared -q               # one package
cd services/frontend && npm run test && npm run build
```

`tests/conftest.py` sets default environment values that match `.env.example`, so tests run against the local compose stack without extra setup. CI runs one job per package (`tests/shared`, `tests/api`, `tests/decoder`, `tests/export`, `tests/rules`, `tests/automation`, `tests/integration`, `tests/mcp`) against Postgres/Timescale and Redis service containers and a MinIO container. A test that reads rows a worker committed in another session must end its own transaction first (`await db.rollback()`): the test session's snapshot predates the commit otherwise.

All tests share one event loop (`asyncio_default_test_loop_scope = session`) because asyncpg connections cannot move between loops. `tests/api/conftest.py` has helpers for actors per role (`actor`, `project_actor`), committed fixture rows and invitations. `tests/api/test_phase1_scenario.py` runs the phase 1 exit criteria end to end.

- Drivers, adapters and rules are tested with recorded fixtures under `tests/fixtures/payloads/`. Add the source of every fixture in a `README.md` next to it.
- API tests use a real Postgres/Timescale container. No mocked SQL.
- Playwright smoke opens every page at 390, 768 and 1440 px.
- Benchmarks (`scripts/benchmark/`) run on demand, not in CI. Results go to `docs/operations/benchmarks.md`.

## Releases

`VERSION` is written and committed before each tag (it does not exist before v0.1.0; the code reports `v0.0.0-dev`). `CHANGELOG.md` has an Unreleased section that becomes the release notes. Servers run tags, never `main`, except a dev server.

## Logging

All services write one JSON object per line to stdout (`LOG_FORMAT=json`, the container default) with `service`, `trace_id` and `request_id` where set; `LOG_FORMAT=text` gives readable lines for development. `docker compose logs -f <service>` follows a service. Uvicorn's own access log is not yet routed through the structured logger.
