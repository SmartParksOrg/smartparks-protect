# LORIOT fixtures

Frame shapes follow the LORIOT application websocket output documentation (`rx`, `gw`, `txd`).
Values are invented around the local simulator's DevEUI. Recorded frames from a LORIOT
application replace these when the live run happens (phase 7 input); keep the origin here.
