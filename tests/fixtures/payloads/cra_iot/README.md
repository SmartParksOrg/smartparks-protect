# CRA IoT fixtures

`envelope.json` is the HTTP endpoint example from the CRA IoT documentation
(github.com/cra-iot/documentation, "Výstup z IoT platformy/HTTP"): the integration envelope
with the deduplicated `gw` message as a JSON string. Recorded messages from a CRA account
replace it when the live run happens; keep the origin here.
