# akenza fixtures

`uplink.json` is the "LoRaWAN Device uplink event" sample from docs.akenza.io (webhook output connector, uplink event structure) with the local simulator's DevEUI as `device.deviceId` and an OpenCollar port 2 frame as `payloadHex`. Recorded samples from an akenza workspace replace it; keep the origin here.
