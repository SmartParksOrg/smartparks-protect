[project]
name = "smartparks-protect-export"
version = "0.0.0"
description = "Smart Parks Protect export service."
requires-python = ">=3.12,<3.13"
dependencies = [
    "smartparks-protect-shared",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "protect_export"
module-root = ""
