[project]
name = "smartparks-protect-rules"
version = "0.0.0"
description = "Smart Parks Protect rules service: evaluates rules, creates events and alerts."
requires-python = ">=3.12,<3.13"
dependencies = [
    "smartparks-protect-shared",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "protect_rules"
module-root = ""
