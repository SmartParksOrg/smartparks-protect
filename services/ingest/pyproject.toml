[project]
name = "smartparks-protect-ingest"
version = "0.0.0"
description = "Smart Parks Protect ingest service."
requires-python = ">=3.12,<3.13"
dependencies = [
    "smartparks-protect-shared",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "protect_ingest"
module-root = ""
